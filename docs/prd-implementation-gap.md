# 기획안 ↔ 구현 대조

**기준**: `docs/prd-vivnanaut-app.md` (2026-08-08 재작성) vs 코드 (**2026-08-14** 갱신, `feat/prd-redesign` @ `eb04a45`)

> 2026-08-14 갱신 메모: 이 기간의 변경은 Liquid Glass 전면 리스킨 + `/design-test` 비로그인
> 공개 + 리브랜딩(스플래시·로그인) — 전부 시각/체험 레이어라 아래 **기능 대조표 내용은 변동 없음**.

기획안 본문에는 구현 이야기를 적지 않는다(기획안 §0.2 "구현 역산 금지"). 대조는 이 문서가 맡는다.

**읽는 법**: ✅ 일치 · 🟡 부분 · ❌ 없음 · ⚠️ **기획을 앞질렀거나 기획에 없는 것**

---

## 요약

| 영역 | 상태 | 한 줄 |
|---|---|---|
| §4.1 홈 탭 | ✅ 대체로 일치 | 라이브 오버레이 3종만 빠졌다 |
| §4.2 타이머 & 일정 | 🟡 **일정만 실물** (2026-08-12) | 반복 예약 CRUD·토글은 동작. **타이머·구간 예약·스마트 조건은 계약 대기** — 화면에 사유를 적어 뒀다. `docs/backend-handoff-timer-mist.md` §10.7 |
| §4.3 통계 탭 | 🟡 **자리는 다 섰다** | §4.3.1~3은 동작(기기 마커 포함), 나머지 5칸은 자리표시자. 기획안 자체가 미확정(§6 D)이라 내용을 지어내지 않았다 |
| §4.4 마이크레 탭 | ⚠️ 기획 앞섬 | 기획은 **탭 존치 여부가 미결**인데 구현은 이미 있다. 시각은 2026-08-09 통일 |
| §4.5 커뮤니티 탭 | 🟡 위키만 실물 | 카테고리 구조 + 위키는 동작. 게시판은 **테이블이 없어** 자리표시자. 지어낸 글 5건은 2026-08-09 제거 |
| §4.6 온보딩 | ✅ 둘 다 없음 | 기획 미설계 = 구현 없음 |
| §2.2 알림 센터 | ❌ 스텁 | 16줄. 카테고리 4종·Red Dot 실데이터 없음 |
| §5 안전 규칙 | 🟡 2/4 | 히터 확인·분무 잠금은 있고, 나머지 둘은 §4.2에 묶여 있어 같이 비었다 |
| **계정 / 내 프로필** | ⚠️ 기획에 없음 | 완성돼 있으면서 진입 불가였다. 2026-08-09 헤더 아바타로 길을 열었다. 아래 별항 |

---

## §4.1 홈 탭

| 기획 | 구현 | |
|---|---|---|
| 헤더 — 세트 드롭다운, 1개면 화살표 숨김 | `HomeHeaderBar` + `ScreenHeader` | ✅ |
| 헤더 — 🔔 미읽음 Red Dot | 표시 로직 있음, **개수가 0 고정**(`unreadNotificationCountProvider`) | 🟡 알림 저장소 대기 |
| 헤더 — ⚙️ 사육장 설정 | `/enclosure-settings` | ✅ |
| 헤더 — 내 계정 아바타 | `AccountAvatar` → `/profile` (2026-08-09) | ⚠️ 기획에 없는 것을 채웠다 → §6 O |
| 상단 — 16:9 라이브 · 좌우 스와이프 · 인디케이터 | `TopFixedArea` + `LiveSurface` | ✅ |
| 상단 — 상태 배지 `LIVE`/`OFFLINE` | `StatusBadge`. 연결 중엔 배지를 내지 않는다(가운데 문구와 중복) | ✅ |
| 상단 — 끊기면 경고 + `[재연결]` | `LiveSurfaceNotice` | ✅ |
| 상단 — 오버레이 **현재 시각** | `LiveClockOverlay` | ✅ |
| 상단 — 오버레이 **전체화면 전환 · 캡처 · 수동 녹화** | 없음 | ❌ |
| 상단 — 캠 없으면 프로필 카드(사진·D-Day·급여일·체중·온습도 배지) | `PetProfileCard` | ✅ |
| 서브탭 전환해도 영상 안 끊김 | `TopFixedArea`를 서브탭 컨테이너 **밖**에 둔다 | ✅ |
| 제어 — 진행 중 타이머 칩 | `RunningTimerChip` | 🟡 위젯은 있으나 타이머 기능(§4.2)이 없어 뜰 일이 없다 |
| 제어 — 실시간 온습도 + 당일 최고/최저 | `LiveEnvCard` | ✅ |
| 제어 — 24h 차트(전날19:00~현재, 기기 마커, 터치→통계) | `EnvMiniChart` → `context.go('/stats')` | ✅ |
| 제어 — 퀵 제어 4종 | `QuickControlGrid` | ✅ |
| 제어 — 타이머 & 일정 진입 버튼 | `/home/routines` | ✅ (목적지는 빈 화면) |
| 분무 5초 잠금 + 완료 토스트 | `mistLockProvider` | ✅ |
| 타임라인 — 요약 칩(밤 22~06 집계) | `TimelineSummaryChips` + `StatRow`, 기준 표기 있음 | ✅ |
| 타임라인 — 날짜 스크롤러(당일 07~07) | `TimelineDateScroller` | ✅ |
| 타임라인 — 필터 칩 고정 노출, 0건은 비활성 | 카운트까지 표기 | ✅ |
| 타임라인 — 클립 피드 | `TimelineClipFeed` (sliver) | ✅ |
| 타임라인 — **전체화면 가로 재생** | `MotionClipPlayerScreen` | ✅ (2026-08-09) |

> **오버레이 3종**이 유일한 실질 공백이다. 전체화면은 라이브에도 필요하다 — 지금 클립만 전체화면으로 볼 수 있고 라이브는 16:9에 갇혀 있다.

## §4.2 타이머 & 일정 — 🟡 일정만 실물 (2026-08-12)

`schedules` 테이블·REST가 생기면서 **§4.2.2 일정의 시점 예약까지** 채웠다.

| 기획 | 구현 |
|---|---|
| §4.2.2 일정 — 목록·추가·수정·삭제·토글 | ✅ `RoutineSettingsScreen` + `ScheduleRepository`(REST 전용) |
| §4.2.2 매일 / 요일 지정 · 시각 | ✅ |
| §4.2.2 분무 분사 시간 | ✅ 1/2/3초 (**PRD 개정** — 원문은 "설정 불가"였다) |
| §4.2.2 LED 밝기 0~100% | ❌ **하드웨어가 못 한다** — 현 보드 LED는 PWM이 아니라 on/off 릴레이. 앱 슬라이더는 아무 효과가 없어 걷어냈다 |
| §4.2.1 타이머 (즉시·일회성) | ❌ 계약 대기 — A안(`fan_on`+`duration_ms`) 결정, 팬 먼저 |
| §4.2.2 시작~종료 구간 예약 | ❌ `schedules` 화이트리스트 확장 대기 (**펌웨어에는 이미 있다**) |
| §4.2.2 스마트 조건 | ❌ 계약 대기 — 스킵형=서버, 정지형=펌웨어로 단계 분리 결정 |
| §4.2.3 작동 기록 | ❌ `commands` 출처 컬럼 미정 → §4.3.8과 묶임 |

> ⚠️ **기획안이 "이 기능의 핵심"이라고 못박은 스마트 조건이 아직 없다.** §5 안전 규칙 4건 중 2건(히터 정지 온도, 팬 과건조 정지)이 여기 묶여 있다.
> 다만 **못 만든 이유를 화면에서 밝힌다** — `RoutineSettingsScreen` 상단 "아직 준비 중인 기능" 카드 3개. 빈 화면은 고장으로 읽힌다.
> 대기 목록 전체: `docs/backend-handoff-timer-mist.md` §10.7

## §4.3 통계 탭 — 🟡 자리는 다 섰다

**2026-08-09 — 기획안 §4.3 순서대로 레이아웃을 전부 세웠다.** 못 만든 칸은 지우지 않고 `PendingSection`으로 둔다.

| 기획 | 구현 |
|---|---|
| §4.3.1 기간 `[일간][주간][월간]` | ✅ 동작. 주간·월간을 고르면 차트 자리가 자리표시자로 바뀐다 |
| §4.3.2 메트릭 필터 다중 선택 | ✅ 온·습도는 실제로 차트를 다시 그린다. 활동량은 비활성(집계 경로 없음) |
| §4.3.3 메인 차트 — 온습도 라인 | ✅ 일간만. 24시간·듀얼축. Figma 실측 치수 기준으로 2026-08-10 재작성, 홈과 공용(`EnvChart`) |
| §4.3.3 활동량 Y2 막대 | ❌ 자리표시자 — 영상 행동 분석 연동 후 |
| §4.3.3 기기 마커 오버레이 | ✅ (2026-08-10) `commands` 이력에서 `status='acked'`만. 분무·환기·히터·조명 |
| §4.3.4 AI 분석 / §4.3.5 주간 행동 / §4.3.6 분포 / §4.3.7 히트맵 / §4.3.8 Audit Log | ❌ 자리표시자 5칸 |

기획안이 스스로 *"확정 스펙이 아니다"*(§6 D)라고 적은 구간이라 **내용을 지어내지 않았다.** 대신 각 칸이 무엇을 보여줄 자리이고 왜 아직 없는지를 화면에 밝힌다 — 그 자체가 검토 가능한 기획서가 된다.

> **`EmptyState`와 `PendingSection`을 갈라 쓴다.** 앞은 *"기능은 있는데 데이터가 없다"*, 뒤는 *"기능 자체가 없다"*. 같은 모양으로 그리면 사용자가 자기 기록이 사라진 줄 안다.

## §4.4 마이크레 · §4.5 커뮤니티 · §4.6 온보딩


### 마이크레 ⚠️ 구현이 기획을 앞질렀다

기획은 *"우선 후순위 / 탭 존치 여부 자체가 미결"*(§6 E)인데 구현은 개체 CRUD + 어젯밤 리포트까지 있다.
PRD §4.4가 나열한 기능(프로필 6항목 · 활동 기록 5종)은 **사실상 다 있다.** 남은 건 기획 결정 — *"통계 통합"* vs *"달력+다이어리 독립"*. **어느 쪽인지 정해지기 전까지 새 화면을 짓지 않는다.**

2026-08-09 시각만 통일했다: 헤더를 `ScreenHeader`로, 뷰 전환을 통계와 같은 세그먼트로.
- 옛 브랜드 초록(`AppTheme.success`) 알약 칩을 걷어냈다 — 그 초록은 이제 "정상" 의미색이라 **선택 상태를 상태값처럼 보이게** 했다
- `_SexBadge`가 `isMale ? 분홍 : 분홍`이라 **암수가 같은 색**이었다. 배지가 성별을 구분하지 못하고 있었다 → 팔레트의 `subBlue`/`subRed`로 분리

### 커뮤니티 🟡 위키만 실물이다

구조(공지·QnA·자유·위키)와 위키 진입 카드는 동작한다. **게시판은 Supabase에 테이블이 하나도 없다** — 읽을 곳도 쓸 곳도 없다.

> ⚠️ **지어낸 글 5건을 제거했다**(2026-08-09). `_seedPosts()`가 가짜 제목·작성자(`초보집사`, `도리도리`)·댓글 수를 넣어 화면을 채우고 있었다. 목업으로는 그럴듯했지만 **사용자에게는 실재하는 커뮤니티로 보였다.** 지금은 `PendingSection`이 무엇이 준비 중인지 밝힌다.
>
> 글쓰기 FAB도 없앴다. 쓸 곳이 없는데 버튼만 있으면 누를 때마다 "준비 중"을 확인하게 된다 — **누르기 전에 알 수 있어야 한다.**

재개 조건: 커뮤니티 테이블 + RLS 마이그레이션. 공유 프로덕션 DB 스키마 변경이라 자동 진행 대상이 아니다.

### 온보딩 ✅

기획도 구현도 없다. 라인업 3종 분기 설계가 먼저.

## §5 안전 규칙

| 규칙 | |
|---|---|
| 히터 조작 전 확인 단계 | ✅ 2단(안전잠금 해제 → 조작 확인). `cage_control_actions.dart` 경유 강제 |
| 분무 1회 = 1분사, 5초 잠금 | ✅ |
| 히터 목표 온도 도달 시 정지 / 팬 과건조 자동 정지 | ❌ §4.2에 묶여 있다 |
| 제어 결과를 사용자에게 알린다 | 🟡 명령 송출 토스트는 있으나 **실제 실행됐는지**(기획안의 "안심용")는 확인 경로가 없다 |

> **안심존 배지 (2026-08-14 정정)**: 홈의 위험/주의 배지는 공백이 아니라 **2026-08-10 사용자
> 결정으로 제거**됐다(CLAUDE.md 온습도 차트 절). 안심존 판정(`classifyComfort`)은 사육장 탭
> 추이 차트에 존치. 과거 이 자리에 있던 "데이터 배선 문제, 가장 시급" 서술은 그 결정 이전
> 기준이라 폐기 — 현재 남은 관련 항목은 위 표의 "제어 결과 확인 경로 없음"뿐이다.

---

## 기획에 없는 것 — 계정 / 내 프로필

`ProfileScreen`은 **완성돼 있다** — 아바타 업로드, 표시명, 사육 경력, 로그아웃. 라우트 `/profile`도 등록돼 있다.
그런데 **앱 어디에서도 그 화면으로 가지 않는다.** (`grep '/profile'` → 라우터 정의 외 진입점 0건)

즉 지금 사용자는 **로그아웃할 수도, 자기 프로필을 볼 수도 없다.**

원인은 기획 공백이다. 기획안 §2.2가 "탭에 속하지 않는 화면"으로 꼽은 넷(알림 센터·사육장 설정·기기 등록·타이머)에 **계정이 없다.** 노션 원문에도 계정 화면이 없어 기획안이 만들어내지 않았고(§0.2), 그 결과 진입점도 안 생겼다.

→ 기획안 §6 O로 등록했고, **2026-08-09 헤더 우측에 아바타를 붙여 길을 열었다.** 다만 계정 화면을 어디에 두는 게 맞는지는 여전히 기획 미결이다 — 지금 헤더는 나머지가 전부 *현재 세트*에 대한 것이라 계정만 축이 다르다.
