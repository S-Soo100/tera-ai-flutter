# Figma 기획 의도·구버전 디자인 점검 — 2026-09-18

- Talk to Figma 채널 `feudbotc`, 파일 `Final Design`. 코드 기준 main `2b6f54c`(0.114.1+287).
- 방법: ① 9개 섹션의 화면 프레임 183개 텍스트를 다시 읽어 9/16 저장본(`2026-09-16-remaining-ui-handoff/specs`)과 비교 ② 프레임 밖 기획 메모 수집 ③ iOS 시뮬레이터(iPhone 17)에 main 최신 빌드를 설치해 실계정 화면을 Figma PNG와 나란히 비교 ④ 레거시 라우트의 UI 진입점을 코드로 확인.
- 점검 전 시뮬레이터에 깔려 있던 빌드는 iOS `0.111.7+283`, Android `0.113.0+285`로 둘 다 main보다 낡았다. iOS만 최신으로 교체했고 **Android는 교체하지 않았다**(이번 비교는 iOS 기준).
- 범위 밖: 로그인 5상태(로그아웃 필요), 기기 추가 BLE 흐름, 개체 추가 폼, 북마크·메모, 가로 플레이어, 커뮤니티 글쓰기. 이 화면들은 이번에 실화면으로 보지 않았다.

## 1. Figma 쪽 변화 (9/16 이후)

- 9/16에 저장한 104개 프레임의 **텍스트는 바뀐 곳이 없다**(차이 7건은 모두 숨김 레이어). 좌표·색 변경 여부는 이번에 재측정하지 않았다.
- 레포 문서에 없던 메모 1건: `1179:4520` **"마이 크레 탭 이름 변경 — MyCre / 후보: MyPet, Activity"**. 앱 탭 라벨은 `MyCre` 그대로다. 기획자가 이름을 고민 중이라는 표시라 결정이 필요하다.

## 2. 기획 의도가 반영되지 않은 곳

| # | 화면 | Figma·기획 메모 | 앱 | 근거 |
|---|---|---|---|---|
| 1 | 알림 설정 `/profile/notifications` | 권한 요청 섹션 `1179:4465`는 "수신 동의 관리"(비바나트 소식·마케팅 수신 동의)를 흐리게 처리. 메모 `1186:4474` "수신 동의 관리는 베타테스트때는 제외" | 두 토글이 활성 상태로 조작된다 | [pushalarm.png](pushalarm.png). CHANGELOG 0.114.0은 "수신 동의 관리는 계속 조작 가능"으로 구현 — 메모와 반대 |
| 2 | 홈 기기 설정 첫 줄 | "기기 예약 설정" (`1107:10445`, `1107:10559`) | "일정 설정" (`redesign_home_schedule_settings`). 들어간 화면 제목은 "기기 예약 설정"이라 앱 안에서도 이름이 둘이다 | [home.png](home.png). 2026-09-14 "문구는 '일정'" 결정 뒤에 나온 재설계 원본이 다른 이름을 쓴다 — 어느 쪽이 맞는지 결정 필요 |
| 3 | 마이크레 주간 그래프 | 막대 위 값 "30m / 2.5h / 3h 20m / 1h"(0 단위 생략), Y축 라벨 한 줄 | 값 "0h 0m · 0h 29m"로 표기, 0인 날도 "0h 0m"이 붙어 서로 겹친다. Y축 "2.5h"·"0.5h"가 두 줄로 꺾인다(전역 글자 1.15배에서 폭 부족) | [mycre2.png](mycre2.png), `activity_week_chart.dart` |
| 4 | 플레이어 세로 상단 휴지통 | `941:1864` 44 프레임, 글리프 17×19 | `redesign_v2/delete.svg`는 44 상자 자산인데 `size: 24`로 그려 글리프가 약 9×10 | [player_trash.png](player_trash.png), `clip_playlist_player_screen.dart:901`, `:1200` |
| 5 | 플레이어 세로 재생바 | 영상 바로 아래 좌우 끝까지, 각진 막대, 진한 빨강 | 좌우 여백이 있고 둥근 막대, 분홍빛 | [player_bar.png](player_bar.png). 색·여백의 원본 수치는 이번에 노드로 재측정하지 않았다(PNG 비교) |
| 6 | 하이라이트 목록 | 2~3일 묶음 헤더 "2026. 8. 28 - 8. 31" + "하이라이트가 도착했어요" 배너 (메모 `1081:4172`) | "9월 13일 밤" 밤 단위 헤더, 배너 없음, 카메라 탭 카드 "업데이트 없음" | [highlights.png](highlights.png). 서버가 공개 배치를 주지 않아 폴백 중(기존 기록된 제약) — 새 결함은 아님 |

## 3. 구버전 디자인이 남은 곳

| # | 위치 | 상태 |
|---|---|---|
| 7 | 커뮤니티 탭 | Figma에 커뮤니티 피드 원본이 없다. 큰 제목 헤더·앰버 FAB·테두리 카드 등 재설계 이전 문법 그대로라 다른 3탭과 헤더 구조부터 다르다. [community.png](community.png) |
| 8 | `/env-settings`(목표 온습도·카메라 180° 회전), `/enclosure-settings` | 라우트는 남아 있으나 **UI 진입점이 없다**(`context.push` 호출 0건). 재설계 헤더의 설정 아이콘은 기기 추가/기기 관리/개체 메뉴만 연다. Figma 기기 상세에도 해당 항목이 없어, 카메라 회전과 목표 온습도는 현재 앱에서 바꿀 수 없다. 목표 온습도의 다른 진입점인 구 `/smart-cage` 카드도 그 화면으로 가는 `push`가 없어 함께 막혀 있다 |
| 9 | `/signup`, `/verify-email`, `/notifications`(받은 알림), `/my-pets/reports`(어젯밤 리포트) | 진입점이 살아 있고 Figma 원본이 없다. 이번에 화면을 열어 보지는 않았다 — 구 디자인일 가능성이 높아 확인 대상 |
| 10 | `CameraLiveArea._EmptyCameraCard` → `/crecam/cameras/pair` | 카메라 0대는 `RedesignEmptyState`가 먼저 처리해 사실상 죽은 코드. 구 페어링 화면으로 가는 경로라 정리 후보 |

## 4. 확인만 하고 결함으로 보지 않은 것

- 마이페이지·내 계정 카드 테두리: PNG에서는 없어 보였으나 노드(`1134:7827`)에 `#E3E3E3` stroke가 있다. 앱이 맞다.
- 마이페이지의 받은 알림·화면 모드·디자인 랩 행, 버전 정보 부제 없음: 9/16 결정표 C1·C7.
- 기기 관리 그룹 라벨 "그룹"(번호 없음): 재설계 RPC 이전에 만든 그룹은 번호가 없다(데이터).
- 홈·카메라·마이크레 상단, 예약 목록(빈 상태), 비밀번호 변경, 기기 관리: 구조 일치.
- 마이크레 일간 그래프의 미도래 시간 "--" 표기, 추정 안내문: Figma에 없지만 9/16 사용자 결정에서 나온 표시.

## 5. 판단이 필요한 관찰 — 2026-09-19 사용자 결정 완료

| 항목 | 결정 | 처리 |
|---|---|---|
| 3번 탭 이름(메모 `1179:4520`, 후보 MyPet·Activity) | **MyCre 유지** | 변경 없음. 기획자 확정 시 재검토 |
| 온습도 상세 Y축 칸 하한 | **지금처럼 데이터에 맞춤** | 변경 없음(1·2·5 계열, 하한 없음) |
| 제어 시트 뒤 화면 딤 | **딤 없이 Figma대로** | `barrierColor` 투명 + 시트 상단 그림자(드롭다운 값 blur20 `#919497` 30% 재사용, 시트 원값은 MCP가 effect를 주지 않아 근사). 바깥 탭 닫힘 유지. 0.114.2+288 |
| 하이라이트·북마크 달력 아이콘 | **원본대로 교체** | `calendar_month` 20 + `#3C3C3C`(원본 `945:4296`, 북마크 `945:4351`·`1084:7291`·`1081:5235` 동일). 남은 차이: 레포 SVG는 개체 폼 노드(1063:6882) export라 선이 약간 굵다 — 하이라이트 노드 SVG는 MCP가 PNG만 돌려줘 디자이너 전달 필요. 카메라 탭 '기간 설정' 빈 달력은 원본 일치 확인. 0.114.2+288 |

아래는 결정 전 관찰 원문이다.

- 온습도 상세 일간 그래프: Figma는 20°~45°·5° 간격 축. 앱은 데이터에 맞춰 26.0~27.0·0.2° 간격으로 좁혀, 0.9° 변화가 급등처럼 보인다([envdetail.png](envdetail.png)). "칸 크기 1·2·5 계열" 규칙 안이지만 최소 칸(예: 1°) 하한을 둘지 결정 필요.
- 제어 시트: 앱은 뒤 화면을 어둡게 덮고, Figma 프레임(`1107:7996`)은 뒤 화면이 그대로 밝다([sheet_fan.png](sheet_fan.png)). 원본이 오버레이를 생략해 그린 것인지 확인 필요.
- 하이라이트·카메라 탭 달력 아이콘이 원본(calendar_month)과 글리프가 달라 보인다 — 확대 비교는 하지 않았다. (2026-09-19 확대 결과: 카메라 탭은 일치, 하이라이트·북마크 상단바만 달랐다.)
