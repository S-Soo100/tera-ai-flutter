# 기기 제어 시트·로그인·마이페이지 Figma 대조 결과 — 2026-09-16

- 작업 트리 `.worktrees/redesign-20260915`, 브랜치 `codex/figma-redesign-20260915`. 계획서 [`docs/superpowers/plans/2026-09-16-next-session-sheets-login-mypage.md`](../../superpowers/plans/2026-09-16-next-session-sheets-login-mypage.md).
- 기준 `052b367`(0.108.7+272) → 로그인 `94fb12d`(0.109.0+273) → 편집기 분리 `e6cbee8`(0.109.1+274) → 제어 시트 `d59d062`(0.110.0+275) → 마이페이지(0.111.0+276). 푸시하지 않았다.
- 비교 자료: `figma/`(talk-to-figma 채널 `z9yk66ru`에서 내보낸 PNG, 시트 프레임은 그림자 여백 20 포함), `after/`(위젯 테스트 캡처 393×852, SafeArea 62/34 + 위젯 rect JSON), `after/*-compare.png`(원본 | 앱 나란히). 캡처는 fixture 값이며 실기기·서버 데이터가 아니다.
- 사용자 확인이 필요하던 항목(계획 §1-3·§2-3·§3-3)은 대화가 불가능한 세션이라 **권장안으로 진행**하고 아래 결정표에 남겼다. "전체 완료"라고 쓰지 않는다 — 서버 의존 3건과 확인 대기 항목이 있다.

## 1. 결과 요약

| 섹션 | 상태 | 픽셀 차이(원본 대비, 상태바 제외) | 테스트 |
|---|---|---|---|
| 로그인 5상태 | 구현 완료 | default 0.71% · typing 1.61% · pw-visible 1.17% · autologin 1.02% · error 2.48% (y62~440) | `login_layout_test` 5건, 캡처 `redesign_login_capture_test` |
| 기기 제어 시트 7상태 | 구현 완료 | fan-off 8.4% · fan-on 8.5% · cooling 11.4% · led-on 7.2% · mist 6.2% · led 예약 편집기 4.0% · cooling 예약 편집기 8.2% (차이는 칩 기본 선택·시트 상단 라운드/그림자·LED 작동 시간 칩 제외분) | `device_control_sheet_test` 9건, `cage_control_grid_test` 12건, `routine_*` 46건, 캡처 P09 |
| 마이페이지 7화면 | 구현 완료(서버 의존 3건 대기) | mypage 3.2% · commu-profile 8.3% · commu-block 1.4% · push-alarm 3.8% · my-account 0.4% · pw-change 0.3% · withdraw 0.4% | `mypage_layout_test` 6건, `profile_screen_test`, 캡처 `redesign_mypage_capture_test` |

전체 `flutter test` 통과(각 커밋 전), `flutter analyze` 에러 0(기존 info 5건).

## 2. 결정표

### 기기 제어 시트 (계획 §1-3)

| # | 결정 | 근거 |
|---|---|---|
| A1' | **전원 스위치가 명시적 시작/정지.** 칩·슬라이더는 선택만(송신 없음). 꺼짐→켜짐이면 고른 값으로 `*_on`, 켜짐→꺼짐이면 `*_off`. 켜진 채 칩을 바꾸면 그 값으로 다시 켠다(타이머 교체), 켜진 LED는 슬라이더에서 손 뗄 때 밝기 재전송 | 계획 A1은 "전원 헤더 표시 전용 + 적용/시작 버튼"이었으나 원본 시트(1106:4111 등)에 시작 버튼이 없고 스위치가 있다. 동작 없는 스위치는 고장으로 읽히고, 별도 버튼은 원본 높이와 어긋난다. 2026-09-14 "선택/취소만으로 송신 안 함"은 지킨다. **사용자 확인 요청** |
| A4 | 타일 부제 카운트다운 "켜짐 · 30m 56s 뒤 꺼짐"(1h 이상은 "1h 20m"), 홈 상단 칩 제거 | 계획 권장안 |
| A5 | 분무는 시트 "1회 분사 시작" → 2초 실행 취소 스낵바 → 3초 분무 → 토스트. 타일 즉시 분사(2026-09-07) 폐지 | 계획 A5. **사용자 확인 요청**(2026-09-07 지시 뒤집음) |
| A6 | 냉각팬 안내문·30분/1시간/2시간 뒤 칩 | 구현 |
| A7 | LED 작동 시간 칩 **없음** | 펌웨어 미지원(A안 확정). 원본 1106:4127과 의도적 차이(led-on 7.2%의 대부분) |
| A8 | 예약 잠금 "예약 일정이 작동 중입니다" **없음** | 서버 진행 중 판정 없음(계획 유지) |
| A9 | 분 화살표 10분 단위 | 디자이너 메모. 예약 설정 화면도 같이 바뀜(테스트 55→50 갱신) |
| A10 | 예약 목록 시작 시각 정렬 | 기존 구현 확인, 시트 목록도 같은 `scheduleRows` |
| A11 | 요일 부제는 기존 "매일 / 토 일" 유지 | 원본 시트 목록(1106:5629)은 "평일·주말"이지만 목록 원본(1106:5317)은 "토 일" — 원본끼리 충돌, 기존 유지 |
| A12 | 예약 탭에서 삭제 없음 | 원본 시트 편집기에 삭제가 없다. 삭제는 예약 설정 화면 |

### 로그인 (계획 §2-3)

| # | 결정 |
|---|---|
| B1 | 공용 `VivaTextField`·`VivaCheckRow` 신설. 오류 문구는 필드 컴포넌트 원본(1133:5771)대로 **오른쪽 정렬·안쪽 12**. 개체 폼·그룹 이름은 아직 미교체(후속) |
| B3 | "자동 로그인"만 채택(끄면 다음 실행에 세션 해제). "비밀번호 기억하기"(일부 프레임)는 비밀번호 로컬 저장이라 채택 안 함. **디자이너 확인 요청** |
| B4 | 회원가입 링크는 CTA 아래 y752 텍스트로 유지. "둘러보기"·"디자인 미리보기"는 원본에 없어 로그인에서 제거(`/design-test` 라우트·마이페이지 개발 빌드 진입은 유지) |
| B5 | 로그인 검증은 이메일 형식 + 비밀번호 6자 이상만. 규칙 문구는 힌트·짧은 비밀번호 오류에만. 서버 인증 실패는 비밀번호 칸 아래 한글 안내 |

### 마이페이지 (계획 §3-3)

| # | 결정 |
|---|---|
| C1 | `/profile` 재작성. 원본 행(알림·내 계정·버전) 좌표 348/420/492 유지, 그 아래 원본에 없는 행 3개: 받은 알림(→ `/notifications`, 미읽음 점), 화면 모드(시트 3택), 디자인 랩(개발 빌드만). **사용자 확인 요청** |
| C2 | 커뮤니티 프로필 `/profile/community`. `experience_hidden` 컬럼은 [초안 SQL](../../../supabase/drafts/20260916_user_profiles_experience_hidden.sql) — 적용 전엔 응답에 키가 없어 체크가 저장되지 않는다(400 방지). 닉네임 중복(23505)은 "중복되지 않는 이름으로 설정해 주세요" |
| C3 | 차단 목록 재스킨(카드 76·"차단됨") + 해제 모달 + 토스트. 부제는 원본대로 "비공개" 고정(차단 목록 조회에 사육 경험이 없다) |
| C4 | 알림 설정 화면 + **Hive 로컬 저장**. 서버 테이블·dispatcher는 [요청서](../../handoffs/2026-09-16-mypage-server-requests.md). 서버 반영 전에는 발송을 막지 못한다 |
| C5 | 내 계정: 로그아웃 CTA → 모달. 비밀번호 변경: 현재 비밀번호는 재로그인으로 검증, 규칙 6~12자 영문+숫자+특수문자(두 칸 빨간 테두리+안내), 불일치·현재 불일치는 모달 |
| C6 | 회원 탈퇴 화면 + `delete-account` Edge Function 호출. 함수 미배포 → "아직 준비되지 않았습니다" 스낵바. 서버 합의 필요 |
| C7 | 버전 정보는 현재 버전만(`0.110.0 (275)` 형식), 최신 판정·스토어 이동은 원격 설정 없어 후속. 원본 모달 문구는 판정 없이 띄우면 거짓이라 미사용 |

## 3. 자산

| 자산 | 처리 |
|---|---|
| vivanaut 워드마크(로그인 로고) | Figma 1134:5937 PNG 3배 → `assets/images/logo_vivanaut_wordmark.png`(200×48.6 표시) |
| person_cancel · notifications · info_i (마이페이지 행 아이콘 24) | talk-to-figma가 SVG 요청에도 PNG를 돌려줘 **4배 PNG**로 보관(`assets/icons/redesign_v2/png/`, `MyPageRasterIcon`). 디자이너 SVG 전달 시 `FigmaIcon`으로 교체 |
| visibility/visibility_off · check_box_400 · add_photo_alternate · check · arrow_next/previous · person | 기존 SVG 재사용 |

## 4. 남은 것 / 확인 요청

1. A1'(스위치=시작) · A5(분무 시트 전환) · B3(자동 로그인 라벨) · C1(원본 외 행 3개) — 사용자 확인.
2. 서버 의존: C2 컬럼, C4 테이블+dispatcher, C6 Edge Function — [요청서](../../handoffs/2026-09-16-mypage-server-requests.md).
3. 개체 폼·그룹 이름 입력의 `VivaTextField` 통일(B1 후속).
4. 실기기: 시트 전원 스위치·분무 실행 취소·비밀번호 변경은 명령·계정에 닿는 동작이라 시뮬/실기기에서 미실행(금지 규칙). 위젯 테스트로 송신 payload만 검증했다.
