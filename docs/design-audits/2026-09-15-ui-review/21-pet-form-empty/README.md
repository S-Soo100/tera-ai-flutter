# 검수25 — 개체 정보 입력 초기 화면

상태: 사용자 수정 승인 후0.107.29+240 구현·재촬영. 아래 최초 대조표는239 수정 전 기록이다.

Figma axfr9s1n /1043:2957(개체 추가_empty)를2026-09-16 신규 조회·export. 원본393×1603/SafeArea top62,bottom34에 맞춰 실제 PetFormScreen을 위젯 테스트로 촬영했다. 별도로393×852에서 상단과 마지막 스크롤을 캡처했다. 앱 캡처에 OS 상태바는 포함되지 않는다.

## 수치 대조

| 항목 | Figma | 앱239 | 판정 |
|---|---|---|---|
| 헤더 | y62,h44/제목16·700·19·자간-.32/#1E1E1E | 동일 | 글꼴 스타일 일치 |
| 뒤로가기 | 터치 x11.94,w44/아이콘 x21.94,w24 | AppBar 기본 leading폭56/아이콘 x16,w24 | 약6pt 왼쪽, 터치영역 구조 차이 |
| 사진 추가 | x106.44,y122,180×180/r12/#E3E3E3 | x106.5,y122,180×180/r12/#E3E3E3 | 프레임 일치 |
| 사진 문구 | y218,h28,16·500/#949090 | 동일 | 일치 |
| 이름·종·모프 라벨 | y326/446/566,16·500·19·자간-.32/#949090 | 동일 | 일치 |
| 이름·종·모프 필드 | y357/477/597,369×65,r12/#FAFAFA | 동일 | 일치 |
| 이름 카운터 | 오른쪽끝363.94,4/10(원본 샘플) | 오른쪽끝360,0/10 | 약4pt 여백 차이. 빈 값0/10은 실제 길이 기준 유지 |
| 성별 | y717,h40,기본 미구분/네이비,텍스트#FAFAFA | 위치·기본선택 동일,선택 글자/체크#FFFFFF | 선택 전경색 보정 필요 |
| 생일·입양일·체중·그룹·메모 라벨 | y781/901/1021/1141/1261 | 동일 | 일치 |
| 그룹 설정 |16·600/#C00306,화살표18×18 x345.94,y1195.5 |16·600/#C00306,화살표24×24 x340,y1192.5 | 화살표 크기·정렬 보정 필요 |
| 메모 필드 | y1292,369×130 | y1292,369×110 | 높이20pt 부족 |
| 저장 버튼 | x11.94,y1434,369×56,r12,초기회색#E3E3E3 | x12,y1414,369×56,r12,검정#1E1E1E·활성 | 메모칸 높이로20pt 위로 이동. 초기 비활성 미적용 |
| 저장 글자 |18·600·줄높이28·자간-.36/#FAFAFA |18·600·줄높이21·자간-.32/#FFFFFF | 스타일 보정 필요 |

색상은 기존 ColorSystem에서 대응할 수 있다. 사진·검색·달력·체크 SVG는 기존 에셋을 사용하며 신규 다운로드 필요 없음. 체크는 원본24pt프레임 안의 내부그림15×11에 대응해 현재24pt컨테이너/15pt SVG를 사용하므로 프레임 수치만 보고24pt로 확대하면 안 된다. 픽셀 단위 획/안티앨리어싱 일치는 별도 판정이며 완료로 단정하지 않는다.

## 저장 버튼·스크롤 확인

원본은 높이1603의 긴 폼이며 메모 뒤12pt에 저장 CTA가 배치되어 있다. 현재 앱은 ListView 내부에 버튼이 있고 실제393×852에서 스크롤 끝에 도달하면 메모와 저장 버튼을 누를 수 있다. 초기 화면에는 버튼이 보이지 않는다. 원본 노드 응답에 prototype의 고정/스크롤 설정이 없어 Figma 런타임 고정 여부까지 검증했다고 보지 않는다. 현재 제안은 긴 폼 흐름을 유지하면서 메모 높이와 버튼 스타일·활성 조건을 원본에 맞추는 것이다.

빈 폼에서도 FilledButton.onPressed가 non-null인 것을 테스트로 확인했다. 현재는 저장을 누른 뒤 필수항목 오류를 표시하는 구조다. 제안: 이름·종 및 입력 유효성 충족 전에는 회색 비활성, 유효 입력 후 활성. 기존 실제 저장 검증·중복 방지·저장중 차단은 유지한다. 실제 서버 저장은 이번 검수에서 수행하지 않았다.

체험 흐름(수정 제안): 빈 폼 진입 → 사진/필수값/선택값 입력 → 스크롤 끝에서 메모와 저장 확인 → 유효하지 않으면 회색, 유효하면 저장 가능 → 저장 처리 중 중복 조작 차단.

원본 초기 프레임의4/10은 샘플 값으로 본다. 앱 빈 이름의0/10을4/10으로 하드코딩하지 않는다. 중복/10자초과 오류, 종·모프 선택, 수정된 값 저장 및 이탈 확인창은 다음 상태별 검수로 분리한다.

## 검증

원본 크기/모바일 크기 캡처 및 마지막 저장 접근성 테스트1개 통과. 임시 테스트는 제거하고 재현용 소스를 문서 폴더에 보관했다. 운영 데이터·제품 코드 변경 없음. 기존239 iOS 빌드 통과, analyzer 오류0·경고0/기존info6 상태를 유지한다.

## 승인 수정 결과 — 0.107.29+240

- 저장 활성: 이름·종 및 기존 이름/체중 검증을 통과하고 저장중이 아닐 때만 활성. 빈 폼은회색#E3E3E3 비활성. 유효 입력 후 활성,10자초과/공백으로 비활성,복구 후 재활성 회귀 테스트 통과. 저장 함수의 기존 검증은 유지.
- 메모 필드369×130,y1292. 저장버튼369×56,y1434. 글자18/600/줄높이28/자간-.36/#FAFAFA,y1448 확인.
- 뒤로가기 SVG24×24,x22,y72(원본x21.94).44pt 조작영역의 왼쪽12pt 확보. 동일 헤더를 사용하는 수정화면 로딩/오류 화면에도 적용.
- 그룹 화살표18×18,x346,y1195.5. 문구와4pt 간격,문구끝x342(원본341.94). 이름 카운터끝x364(원본363.94).
- 빈 필드 suffixText는 텍스트가 없는 상태에서 그리지 않는 Flutter 동작도 확인했다. 항상 보이는 suffixIcon 슬롯으로 전환해0/10을 표시한다. 값은 실제 글자수를 사용한다.
- 선택 글자/체크#FAFAFA. 재측정 결과 기존 FigmaIcon size15는 높이15로 해석되어 내부20.45×15였음. 앞선 기록의 ‘15×11에 대응’이라는 설명을 정정한다. size11로 수정하여24×24컨테이너 안 내부15×11을 확인했다.
- 입력칸·목록·메모·버튼은 기존 하나의 스크롤을 유지. 실제393×852 스크롤 끝에서 저장 버튼 접근 가능. 원본 prototype고정설정은 여전히 미확인.
- 관련 영구 테스트13개+재촬영/스크롤 테스트1개=14개 통과. 실패 저장 입력 보존 포함. 임시 캡처 테스트 제거 및 소스 보존. 제품 위젯 캡처이며 실제 서버 저장은 수행하지 않았다.

원본 분수 좌표 및 글꼴폭의1pt미만 차이는 남는다. 전체 픽셀 일치 판정은 하지 않았다.

최종검증: iOS Simulator debug 빌드 성공. flutter analyze 오류0·경고0/기존info6.14개 테스트 통과.
