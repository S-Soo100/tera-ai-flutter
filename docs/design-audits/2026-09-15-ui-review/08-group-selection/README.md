# 검수 12 — 그룹 구성원 추가·변경

상태: **수정 방향 승인 → 0.107.19+230 반영, 수정 화면 사용자 재확인 대기**.

Figma axfr9s1n 987:5094를 다시 조회했다. 구성원 선택 단계에 빠져 있던 종류별 목록 배경과 둥근 외곽을 적용했다. 기존 ColorSystem의 `context.glass.overlay`(밝은 테마 #F4F4F4), radius 12와 클리핑을 사용한다. 그룹 상세 단계와 같은 표현이다.

체험 흐름: 추가·변경 진입 → 사육장·카메라·개체별 회색 목록을 확인 → 항목을 누르면 체크 표시가 전환 → 선택 내용을 구별할 수 있다. 선택 정책과 저장 동작은 변경하지 않는다.

원본에서 행 64pt, 목록 반경 12pt, 제목 아래 간격 12pt, 종류별 목록 사이 24pt, 행 좌우 패딩 16pt, 원형 아이콘 36pt/내부 SVG 20pt, 체크박스 24pt를 확인했다. 현재 기본 행은 최소 높이 64pt이며 긴 이름은 줄바꿈에 따라 높이가 늘어날 수 있다. 체크박스는 기존 check_box_400/check_box_outline_blank_400 SVG 24pt를 유지했다.

## 비교할 때 구분할 사항

- 원본은 사육장 3개·카메라 2개·개체 2개의 미선택 예시다. 실제 시뮬레이터 QA는 종류별 1개가 기존 그룹 구성원으로 선택된 상태다. 목록 길이, 이름, 체크 유무, 버튼 활성 색 차이는 이 데이터/상태 차이에서 발생한다.
- 이 화면의 ON 표시는 기존 확정 정책을 유지한다. 실제 온라인 상태 검수로 해석하지 않는다.
- `12_group_selection.png`: 수정 전 실제 시뮬레이터.
- `12_group_selection_fixed.png`: 수정 빌드 실제 시뮬레이터 402×874pt / 1206×2622px.
- `13_group_selection_unchecked.png`: 카메라 행을 눌러 체크 해제한 실제 시뮬레이터. 서버 저장은 실행하지 않았다.
- `14_group_selection_widget.png`: Figma와 같은 393×852pt의 제품 위젯 캡처. 카메라 두 행의 구분선과 선택/미선택 표시를 확인하는 보조 자료이며 실제 시뮬레이터 캡처가 아니다.
- 하단 가상 데이터 표시는 QA 전용이다.

## 검증

관리 화면 캡처·화면·컨트롤러 테스트 17개 통과. `flutter analyze --no-pub` 오류 0·경고 0, 기존 info 6개. iOS 시뮬레이터 debug 빌드 성공 후 재설치해 캡처를 확인했다. 전체 화면의 픽셀 일치나 서버 구성원 저장 검증 완료를 뜻하지 않는다.

## 선택 버튼 구조 추가 확정 — 0.107.19+230

사용자 승인: Figma처럼 선택 버튼을 스크롤 콘텐츠 위에 고정한다. 기존에는 Column에서 ListView 뒤에 버튼 공간을 따로 확보했지만, 구성원 선택 단계는 Stack의 Positioned 버튼으로 변경했다. 화면 아래 100pt 위치(버튼 하단)를 기존과 동일하게 유지하고 SafeArea를 반영한다. 마지막 항목을 버튼 위로 올려 조작할 수 있도록 목록 끝에 여백을 둔다. 0개 선택 시 비활성, 1개 이상이면 활성 조건을 유지한다.

새 QA는 Figma와 같은 항목 수(사육장 3·카메라 2·개체 2)로 바꿔 겹침을 확인했다. 이름과 기존 선택 상태는 다르다.

- `15_group_selection_floating.png`: 0.107.19+230 실제 시뮬레이터. 개체 목록 위에 버튼이 겹친다.
- `16_group_selection_floating_widget.png`, `17_group_selection_scrolled_widget.png`: 393×852 제품 위젯의 스크롤 전후. 버튼은 동일한 y=696~752pt, 목록만 이동한다. 두 이미지는 실제 시뮬레이터 스크롤 캡처가 아니다. CUA 스크롤/드래그로 시뮬레이터의 목록 이동이 확인되지 않아 자동 위젯 드래그로 검증했다.
- 회귀 테스트는 스크롤 뷰가 버튼 아래까지 이어짐, 버튼 위치 고정, 마지막 항목 접근/탭, 선택 0↔1 활성 조건을 검증한다. 변경 전 뷰 하단 696pt에서 실패하고 수정 후 통과했다.
- 관련 회귀 테스트 17개 + 별도 캡처 테스트 1개 통과. iOS simulator debug 빌드 성공. 분석 오류·경고 0, 기존 info 6개.

다른 페이지의 버튼도 원본별 스크롤 내부 / 별도 고정 / 콘텐츠 위 플로팅 구조를 구분하도록 CLAUDE.md 및 REVIEW_STATUS.md에 확인 항목을 기록했다. 다른 모든 페이지의 검수 완료를 뜻하지 않는다.
