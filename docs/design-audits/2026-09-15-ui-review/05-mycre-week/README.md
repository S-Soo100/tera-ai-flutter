# 검수 07 — MyCre 주간 그래프

상태: **07 주간 그래프 배치·최고값 라벨 색상 확정**.

현재 코드 0.107.12+223의 실제 MyCre 위젯을 Flutter 위젯 테스트로 렌더링했다. 시뮬레이터 캡처가 아니다. 폭 393pt, 가상 데이터 2026-09-07~13, 선택 주 합계 34h 17m. 캡처 테스트 통과. 재현용 테스트는 capture_test.dart.txt로 보존하며 앱 코드는 변경하지 않았다.

확인 범위: 주간 날짜/요약, 월~일 7개 막대, 값 라벨의 배치, 최고 막대 빨강·나머지 회색, 오른쪽 시간축. 평균 --의 원인과 개선은 사용자 요청으로 전체 UI 검수 종료 후 별도 논의한다(상위 REVIEW_STATUS.md).

Figma 원본 노드 1081:4647, 프레임 크기 369×256, 막대 기본 폭 28. axfr9s1n에서 내보내기 성공했으나 상위 프레임 클리핑으로 370×71 이미지만 제공됨. 원본 전체를 나란히 비교했다고 주장하지 않으며, 클리핑된 이미지도 근거로 보존한다.

위젯 캡처에는 운영 앱의 하단 네비게이션/기기 상태바가 없다. 하단 카메라 귀속 안내와 이전 리포트 버튼은 다음 별도 안건이며, 이번 07 그래프 승인과 묶지 않는다. 가상 값과 실시간 서버 정확도는 UI 배치 승인에 포함하지 않는다.

## 2026-09-16 최고값 라벨 수정

사용자는 최초 승인 직후 “아 잠깐”으로 최고값 텍스트도 빨간색이어야 한다고 수정 요청했다. 당시 07 최종 승인을 보류했다.

주간 그래프를 보면 최고 막대와 바로 위 시간 텍스트가 동일한 ActivityColors.peak로 함께 강조된다. 최고값이 같은 막대가 여러 개면 기존 막대 판정과 동일하게 각 라벨도 빨간색이다. 일반값은 기존 회색이다.

0.107.13+224. 새 위젯 캡처 07_mycre_week_widget_fixed.png에서 5h 30m 텍스트 빨강 확인. 정적 분석 오류 0·경고 0(기존 info 6개), 기존 MyCre 화면 및 캡처 테스트 6개 통과. 시뮬레이터 앱 재설치는 이번 변경에 수행하지 않았다. 평균 표시 문제는 검수 후 재논의한다.

## 사용자 최종 확정

2026-09-16 사용자가 최고값 텍스트 색상 수정 화면에 대해 “확정”했다. 07 현재 그래프 배치 및 최고값 막대·텍스트 동일 빨강을 확정한다. 원본 전체 이미지 대조 미완료 사실은 유지하며 평균 기능과 하단 안내문은 이 승인에 포함하지 않는다.

## 08 설명문 삭제 완료 및 최저 막대 색상 재확인

2026-09-16 사용자가 카메라 귀속 문구 삭제 승인. 실제 화면에서 Text를 제거했다. 활동 집계 및 이전 야간 리포트 접근은 유지한다. 0.107.14+225, 관련 화면/위젯 캡처 테스트 6개 통과, analyze 오류 0·경고 0(기존 info 6개). 최신 위젯 캡처: 08_mycre_footer_removed_widget.png. 시뮬레이터 설치는 수행하지 않았다.

추가 사용자 질문에 따라 axfr9s1n에 재접속해 원본 노드를 읽었다. 일간 1081:4617, 주간 1081:4695 막대는 #B4AEAE. 주간 최저값 텍스트 1081:4694는 #626262다. 현재 앱은 최고 외 모든 막대를 bodySecondary로 표시하여 최저 연회색 구분이 누락되어 있다. 기존 VivaColors.fillIcon이 #B4AEAE와 일치한다. 색상 수정은 이번 문구 삭제 변경에 포함하지 않았으며 후속 수정 항목으로 기록한다. 06/07 기존 배치 승인은 유지하되 최저값 색상은 재검수 항목이다.

## 2026-09-16 최저값 색상 수정 승인 반영

사용자의 “승인”에 따라 일간·주간 최저 막대 색상을 기존 glass.deviceOff 역할색으로 적용했다. 라이트 모드 원본값은 VivaColors.fillIcon #B4AEAE다. 최고값 빨강 판정이 우선하며, 동률 최저값은 모두 연회색이다. 누락(null)은 최저값 계산에서 제외하고 유효 0은 포함한다. 0의 높이를 임의로 키우지 않는다. 최고·최저가 같은 양수는 기존 최고값 빨강을 유지한다. 주간 최저값 텍스트는 일반 진회색이다.

체험: MyCre 일간·주간 그래프를 보면 최고 막대는 빨강, 최저 막대는 연회색, 일반 막대는 진회색으로 구분된다.

0.107.15+226. 새 위젯 캡처 06_mycre_day_minimum_fixed.png 및 07_mycre_week_minimum_fixed.png에서 확인했다. 기존 화면·집계 및 캡처 테스트 13개 통과. analyze 오류 0·경고 0(기존 info 6개). 임시 캡처 실행 파일은 제거했고 원본 재현 스크립트는 기존 txt에 보존한다. 시뮬레이터 앱 재설치는 이번 변경에 수행하지 않았다.

## 09 이전 리포트 버튼 제거 — 2026-09-16

사용자가 제거를 확정했다. MyCre의 개체 등록/미등록 화면에서 이전 야간 리포트 버튼 및 불필요해진 콜백을 제거했다. 기존 리포트 라우트/저장된 데이터는 삭제하지 않았다. 0.107.16+227. 관련 테스트 6개 통과, analyze 오류 0·경고 0(기존 info 6개). 09_mycre_legacy_button_removed.png는 최신 위젯 캡처이며 버튼이 없는 것을 확인했다. 현재 스크롤 끝의 위치는 콘텐츠 길이가 줄면서 이전 캡처와 다르다.

사용자는 “그 카메라의 그래프를 보여주는 편이 직관적이지 않나”라고 제안했다. 현재 그룹에 연결된 카메라의 기록을 보여주는 방향을 추천하며, 카메라 교체 시 과거 날짜도 새 현재 카메라 기준으로 바뀌는 정책을 확인할 예정이다. 조회 가능한 기록을 개체 등록/연결일로 자르지 않는 방안을 제안했으나 집계/조회 로직은 아직 변경하지 않았다. 평균 표시 및 기존 기록 연속성은 검수 후 안건이다.

## 현재 카메라 기준 전환 — 2026-09-16 승인 반영

사용자가 정책을 승인했다. MyPetsScreen에서 선택 개체의 현재 그룹에 속한 카메라 UUID를 찾아 시작·종료 제한 없는 표시 범위를 만든다. ActivityOrigin.currentCamera로 명시하며 실제 연결 이력/과거 소속을 새로 쓰지 않는다. pet_camera_assignments 읽기 의존성을 제거했다. 카메라 목록 로딩/실패는 미연결로 간주하지 않고 오류 시 카메라 목록 재시도를 제공한다. 오프라인 카메라도 연결돼 있다면 과거 기록을 조회한다.

기존 테스트 실패로 이전 동작을 확인한 뒤 수정했다. 새 화면 테스트는 다른 그룹 카메라 제외·현재 카메라 등록 이전 날짜 조회·A→B 교체 후 같은 날짜 유지·연결 해제 시 조회 없음 및 연결 안내를 검증한다. Provider 테스트는 같은 개체/날짜에서 A의 지연 응답이 B를 덮지 않음을 검증한다.

0.107.17+228. 관련 테스트 17개 및 이후 개체 기능 전체 59개 통과, 캡처 opt-in 1개 skip. analyze 오류 0·경고 0(기존 info 6개). UI 형상은 변경하지 않았고 이번 정책 전환에는 새 시뮬레이터 캡처/운영 서버 실측을 수행하지 않았다. 평균 계산 정책은 그대로다.
