# 다음 세션 계획 — 기기 예약·컨트롤 바텀시트, 로그인, 마이페이지 (Figma 대조)

> **2026-09-16 저녁 구현 완료(0.111.0+276).** 결과·결정표·비교 PNG는
> [`docs/design-audits/2026-09-16-sheets-login-mypage/RESULTS.md`](../../design-audits/2026-09-16-sheets-login-mypage/RESULTS.md),
> 서버 의존 3건(experience_hidden 컬럼·notification_preferences+dispatcher·delete-account 함수)은
> [`docs/handoffs/2026-09-16-mypage-server-requests.md`](../../handoffs/2026-09-16-mypage-server-requests.md).
> 아래 "사용자 확인 필요" 항목은 권장안으로 진행했고 RESULTS §2·§4에 확인 요청으로 남겼다.

작성: 2026-09-16, Claude (사용자 지시 "신규 세션에서 세 섹션 수정 계획")

## 0. 시작 전 전제

- 작업 트리: `/Users/baek/myProjects/tera-ai-flutter/.worktrees/redesign-20260915`, 브랜치 `codex/figma-redesign-20260915` (origin과 동일, push 허용됨).
- 현재 버전 0.108.7+272. 서버 계약은 [최종 현황](../../references/2026-09-16-backend-final-status.md)대로 전부 배포·확인됨. 이 계획은 **서버 변경 없이 앱 UI만** 바꾼다.
- Figma: 채널 `z9yk66ru` (세션마다 `bunx cursor-talk-to-figma-socket` 기동 + `join_channel` 필요). 페이지 "Final Design". 프레임 393×852, SafeArea 62/34, 플로팅 CTA y696(+752).
- 검수 규칙: CLAUDE.md "페이지별 필수 수치 검수"·"버튼 구조 검수" 그대로. 캡처 테스트는 `--dart-define=CAPTURE_REMAINING=true` 문법(`test/design/remaining_ui_capture_test.dart`)을 재사용해 `/private/tmp/…/after/`에 저장하고 Figma PNG와 픽셀 대조.
- 금지: 실기기 명령 전송(LED/팬/분무 실제 켜기), 운영 DB 변경, push는 사용자 지시 시. 비밀번호·secret 입력 금지.
- **이전 결정 뒤집기**: 2026-09-16 RESULTS §10-2·§10-7의 "즉시/예약 segment·전원 스위치·시트 예약 탭 = 기존 유지"는 사용자가 이번 지시로 철회했다("바텀 시트로 구성돼 있는데 하나의 페이지로 만들었고 UI도 다르다"). 이 계획은 Figma 시트 구조를 따른다.

## 1. 기기 예약·컨트롤 (Figma 섹션 `1107:7995`, Home 섹션 안)

### 1-1. Figma 구조 (3그룹, 텍스트 스캔 기준)

| 그룹 | 프레임 | 내용 |
|---|---|---|
| 바텀 시트 컨트롤 | Home `1107:7996`·`1107:8134`·`1107:8292`·`1107:8469`·`1106:6790`·`1106:6646` + BottomSheet 18개(`1106:4111`, `1107:8102`, `1106:4127`(LED), `1106:6235`, `1106:5648`(예약 탭 LED, 700h), `1106:4890`(예약 탭 냉각팬, 531h), `1106:4394`, `1106:4642`, `1106:6302`, `1106:5524`(601h), `1106:6415`, `1106:6527`, `1106:6362`, `1106:4296`, `1106:4322`, `1106:6391`, `1106:6605`, `1106:6939`, `1106:5438`, `1106:5629`, `1106:5734`, `1106:6782`, `1106:6885`) | 타일 탭 → 시트. 상단 **segment [즉시 작동 \| 예약 작동]**. 헤더 "전원 · 꺼짐/켜짐". 즉시 탭: LED=밝기 행 + 작동 시간 칩(30분/1시간/2시간/3시간/계속), 환기팬=작동 시간(10분/30분/1시간/2시간/계속), 냉각팬=종료(30분 뒤/1시간 뒤/2시간 뒤) + 안내 "아이스팩은 팬이 켜진 상태에서 최대 2시간 유지됩니다", 분무="1회 분사 시작". 예약 탭: 시작(오전/오후·시·분)·종료·밝기·반복(월~일)·"예약 저장", 또는 그 기기 예약 목록("12:00 평일 · 켜짐", "12:00~14:00 주말 · 켜짐") + "새 예약 추가". 잠금 상태 "예약 일정이 작동 중입니다 / 예약을 일정을 중지하면 즉시 작동 기능을 다시 사용할 수 있습니다" |
| 기기 설정에서 예약 추가 | `예약 설정` `1106:4955`·`1106:5317`·`1107:10307`, `예약 설정_Empty` `1106:7142`, `예약 상세` `1106:7234`·`1107:8758`·`1107:9131`·`1107:9236`, `예약 수정` `1107:9325`·`1107:9413`·`1107:9509`·`1107:9578` | 전체 화면. **현재 앱 P10 구현과 같은 구조** — 기기 선택 → 편집기 → 목록. 예약 수정에 "예약 삭제" |
| 예약 삭제 | `삭제` `1107:10246`, `Modal- 삭제` `1107:9697` | 삭제 모드 "2개 항목 삭제", 확인 모달 345×144 — 현재 앱과 동일 |

타일(`Button/Abled·Disabled·Enabled`): "켜짐 · 30m 56s 뒤 꺼짐", "1h 20m 뒤 꺼짐"(타이머 카운트다운을 **타일에** 표시), LED "켜짐 · 50%", 분무 "작동 중". 토스트 "분무가 실행되었습니다 / 분무 실행을 취소했습니다 / 분무 실행 실패, 다시 시도해 주세요", 스낵바 "잠시 후 분무가 실행됩니다 · 2초 · 실행 취소".
디자이너 메모: "시각 설정 분: 10분 단위 ±", "타이머·예약 사용 시 종료 카운트다운 표시", "예약 작동 중에는 즉시 작동 비활성화", "분무 버튼 로직: 터치→비활성→스낵바 3초→완료 토스트→재활성", "예약 목록 정렬: 시작 시각".

### 1-2. 현재 앱 (0.108.7)

- 시트: LED(`openLedSheet`/`LedControlSheet` — 밝기 행 + 켜기/끄기 버튼), 팬·냉각팬(`FanDurationSheet` — 10분/30분/1시간/2시간 + 시작), 분무는 시트 없이 타일 탭=3초 즉시 분사(2026-09-07 지시). segment·전원 헤더·예약 탭 없음.
- 타이머 진행은 홈 상단 `RunningTimerChip` 한 줄("팬 30분 타이머 가동 중 (18분 20초 남음)"), 타일에는 없음.
- 예약: `/home/routines` 전체 화면(P10) — Figma "기기 설정에서 예약 추가" 그룹과 일치. 편집기 `ScheduleEditorScreen` 섹션(시간 행·칩·밝기·반복)은 시트에 그대로 옮길 수 있게 위젯화돼 있음(`_Section`, `_TimeRow`, `_Chip`, `LedBrightnessRow`).
- LED 작동 시간 칩: 서버 미지원으로 A안 제거(0.108.2). Figma에는 남아 있음.

### 1-3. 차이와 결정

| # | 항목 | 처리 |
|---|---|---|
| A1 | 시트 상단 segment [즉시 작동 \| 예약 작동] + 헤더 "전원 · 상태" | **구현.** 즉시 탭은 현행 송신 규칙 유지(선택/취소만으로 송신 안 함, "적용/시작"으로 송신). 전원 헤더는 텔레메트리 표시 전용, 스위치 아님(2026-09-14 결정 유지) |
| A2 | 예약 탭 = 그 기기의 예약 목록 + "새 예약 추가" + 인라인 편집기 | **구현.** `ScheduleEditorScreen` 내부 섹션을 시트용 위젯(`ScheduleEditorBody`)으로 추출해 재사용. 저장 계약(`addSpan`/`payload`)은 그대로. 목록 행은 `_ScheduleRow` 재사용 |
| A3 | 시트 높이 | Figma 값 그대로: LED 즉시 374, 팬 300, 냉각팬 386(안내문 포함), 분무 176/275, 예약 탭 485~700. 내용 높이에 따라 `DraggableScrollableSheet` 대신 고정 높이 + 내부 스크롤 |
| A4 | 타일 카운트다운 "켜짐 · 30m 56s 뒤 꺼짐" | **구현.** `runningTimersProvider`를 타일이 읽어 부제로 표시. 상단 칩은 Figma에 없으니 제거 여부 사용자 확인(권장: 제거) |
| A5 | 분무 시트 "1회 분사 시작" + 스낵바 2초 "실행 취소" + 토스트 3종 | **구현.** 실행 취소 창(2초) 동안은 명령을 보내지 않고, 만료 시 `mist` 3000ms 송신. 취소 시 토스트 "분무 실행을 취소했습니다". 타일 탭 즉시 분사(2026-09-07)는 시트 진입으로 바뀜 — 사용자 확인 필요 |
| A6 | 냉각팬 안내문 "아이스팩은 팬이 켜진 상태에서 최대 2시간 유지됩니다", 종료 칩 30분/1시간/2시간 뒤 | 구현(문구 ko.json) |
| A7 | LED 즉시 탭 작동 시간 칩 | **넣지 않음**(A안 확정, 펌웨어 미지원). Figma와 의도적 차이로 RESULTS에 기록 |
| A8 | 예약 잠금("예약 일정이 작동 중입니다") | **넣지 않음**(서버에 진행 중 판정 필드 없음, 2026-09-16 결정 유지). 시트에서 그 자리는 비움 |
| A9 | 분 단위 10분 ± | 현재 편집기는 5분 단위. Figma 메모대로 **10분**으로 변경 여부 사용자 확인 |
| A10 | 예약 목록 정렬 시작 시각 | 현재 구현 확인 후 필요 시 정렬 |

### 1-4. 구현 단계 (Standard, 커밋 단위)

1. `ScheduleEditorScreen` → `ScheduleEditorBody`(순수 위젯) 추출 + 기존 화면은 래퍼. 테스트 유지.
2. 공용 `DeviceControlSheet`(segment·헤더·즉시/예약 탭 컨테이너) 신설, LED·팬·냉각팬·분무 4종 즉시 탭 이식. 캡처 비교 `1106:4111/4127/6362/6235` 등.
3. 예약 탭: 기기별 목록 + 새 예약 추가 + 인라인 편집기. 캡처 비교 `1106:5524/5648/4890/6415`.
4. 타일 부제 카운트다운 + 상단 칩 정리(결정 A4).
5. 분무 시트·스낵바·토스트(결정 A5).
6. 시뮬레이터 확인은 **명령 전송 없이** 시트 열기·탭 전환·편집기까지. 예약 저장은 실제 서버 CRUD라 저장 후 삭제로 검증(기기 명령 아님).

## 2. 로그인 (Figma 섹션 `1063:7858`)

### 2-1. Figma

- `Login` 프레임 5개(`1133:5712` 기본, `1134:6114` 입력 중, `1134:6259` 비밀번호 표시, `1134:6472` 자동 로그인 체크, `1134:6617` 오류). 로고 `vivanaut` 상단, 필드 "아이디"(플레이스홀더 vivanaut@gmail.com)·"비밀번호"(영문+숫자+특수문자 6자~12자, 눈 아이콘), 체크박스 "자동 로그인" / "비밀번호 기억하기"(프레임마다 라벨이 다름 — 디자이너 확인), CTA "로그인" y696(비활성 회색 → 활성 검정). 오류: 필드 빨간 테두리 + 12pt 안내("올바른 이메일 형식을 입력해 주세요", "영문+숫자+특수문자 6자~12자").
- `필드 컴포넌트` 섹션 `1134:6053`: 기본/입력 중/오류/카운트(3/10)/비밀번호 4상태. **개체 폼·그룹 이름 입력과 공용**이어야 함.
- 회원가입·비밀번호 찾기 화면 **없음**.

### 2-2. 현재 앱

- `login_screen.dart`(202줄, 구 Material 디자인): 이메일/비밀번호 TextFormField, 표시 토글, 회원가입 링크(`/signup`). 자동 로그인 없음. `signup_screen.dart`, `email_verification_screen.dart` 존재.

### 2-3. 결정·단계

| # | 항목 | 처리 |
|---|---|---|
| B1 | 공용 `VivaTextField`(라벨·필드·오류·카운터·비밀번호) 신설 | 구현. 개체 폼·그룹 이름 입력도 이후 이 컴포넌트로 통일(이번엔 로그인만, 후속 메모) |
| B2 | 로그인 화면 재작성 | 구현. 로고 에셋은 Asset_v2 확인(`logo_vivanaut_verti_260711` 있음) |
| B3 | 자동 로그인 vs 비밀번호 기억하기 | **사용자·디자이너 확인.** Supabase 세션은 기본 유지되므로 "자동 로그인"은 세션 유지 on/off, "비밀번호 기억하기"는 저장 금지(비밀번호 로컬 저장 안 함). 권장: "자동 로그인"만 |
| B4 | 회원가입·비번 찾기 진입점 | Figma에 없음. **결정 필요**: 기존 `/signup` 링크를 로그인 하단 텍스트로 유지(권장) / 숨김 |
| B5 | 유효성 규칙 | 이메일 형식, 비밀번호 6~12자 영문+숫자+특수문자. 서버(Supabase) 정책과 충돌 시 서버 오류 문구는 필드 아래 안내로 |

캡처 비교 5장. `kPublicPaths` 유지.

## 3. 마이페이지 (Figma 섹션 `1142:8859`)

### 3-1. Figma (8프레임 + 모달)

| 프레임 | 내용 |
|---|---|
| `MyPage` `1142:8860`·`1142:8956` | 헤더 "마이 페이지". 커뮤니티 설정: 프로필 카드(닉네임 "자동생성닉네임"/"레이 토마토", 사육 경험 "입문자"·"비공개" 배지, 부제 "사육 경험 정보를 추가해 주세요"), "차단한 사용자 · 내가 커뮤니티에서 차단한 사용자 목록 · 0명". 앱 설정: "알림 · 하이라이트 영상, 커뮤니티 알림 종류 선택", "내 계정 · 비밀번호 변경, 로그아웃", "버전 정보 · 최신 버전으로 업데이트하기 · 0.90.3 (173)" → 최신이면 모달 "현재 최신 버전을 사용하고 있습니다", 아니면 스토어 이동 |
| `Commu_Profile` `1142:9369` | 닉네임(4/10), 사육 경험 3택(입문자 1년 미만 / 중급자 1~3년 / 숙련자 3년 이상), 체크 "커뮤니티에 내 사육 경험 비공개", 저장 |
| `Commu_block` `1142:9422` | 차단 목록 행(닉네임·차단됨) + "차단 해제" → 모달 "차단을 해제하시겠습니까?" → 토스트 "차단 해제 완료" |
| `PushAlarm` `1142:9613` | 기능 설정 토글: 하이라이트 영상 / 커뮤니티 댓글 / 커뮤니티 좋아요. 수신 동의 관리: 비바나트 소식(운영 이벤트) / 마케팅 수신 동의(동의 일자 표기 12pt) |
| `MyAccount` `1134:7760` | 비밀번호 변경 · 회원 탈퇴 · 로그아웃(빨간 CTA) + 모달 "로그아웃 하시겠습니까?" |
| `MyAcc_pwchange` `1134:7865`·`7996`·`8288`·`8076`·`8146` | 현재/새/확인 3필드, 규칙 문구, 오류 모달 "현재 비밀번호가 일치하지 않습니다" / "새로운 비밀번호가 일치하지 않습니다", 조건 불충족 인라인 |
| `MyAcc_withdraw` `1142:8361` | "회원 탈퇴를 진행하시겠습니까? 기기 및 개체의 데이터도 모두 삭제되며 복구할 수 없습니다" + 빨간 CTA |
| 메모 | "구현 기능에 따라 뺄 거 빼면 됨", "이용약관·개인정보·라이선스 페이지는 지금 없음", 오류 케이스 2종 |

### 3-2. 현재 앱

- `profile_screen.dart`(344줄, 구 디자인): 아바타·표시 이름·사육 경험 3택(`user_profiles.experience`)·화면 모드·알림 진입·차단 사용자(`blocked_users_screen.dart`, `community_blocks`)·차트 랩·로그아웃. 라우트 `/profile`.
- 데이터: `user_profiles(display_name, experience, …)`, `community_blocks`, `push_devices(enabled)`. **없는 것**: 사육 경험 비공개 플래그, 알림 종류별 설정, 마케팅 수신 동의 이력, 회원 탈퇴 API, 비밀번호 변경 UI(Supabase `updateUser`로 가능).

### 3-3. 결정·단계

| # | 항목 | 처리 |
|---|---|---|
| C1 | MyPage 목록 화면 재작성(`/profile` 유지) | 구현. 화면 모드·차트 랩 진입은 Figma에 없음 → 사용자 결정(권장: 화면 모드는 앱 설정 하단에 유지, 차트 랩은 dev 빌드만) |
| C2 | 커뮤니티 프로필 편집 | 구현. 닉네임=`display_name`(10자·중복 검사는 서버 규칙 확인 필요), 사육 경험=`experience`, **비공개 플래그 컬럼 신규**(`user_profiles.experience_hidden` 등, 앱 팀 소유 테이블 → 앱 팀 마이그레이션. `public_profiles` 뷰 반영) |
| C3 | 차단 사용자 | 기존 화면을 Figma 규격으로 재스킨 + 해제 모달·토스트 |
| C4 | 알림 설정 | **저장처 결정 필요.** 종류별 on/off는 앱 팀 테이블 신규(`notification_preferences(user_id, highlight, comment, like, news, marketing, marketing_agreed_at)`) + dispatcher가 발송 전에 조회하도록 `dispatch-push` 수정. 마케팅 동의 일자 표기 필수(광고성 정보 수신 동의 기록) |
| C5 | 내 계정 | 비밀번호 변경 = Supabase `auth.updateUser(password)` + 현재 비밀번호 검증은 재로그인(`signInWithPassword`)으로. 로그아웃 모달 |
| C6 | 회원 탈퇴 | **서버 의존.** 클라이언트가 `auth.users`를 지울 수 없음 → Edge Function(service role) 신설 필요. 이관훈님 회신대로 `devices/cameras/enclosures/motion_clips`는 CASCADE, 앱 팀 테이블도 B3에서 CASCADE 적용됨. 기기 MQTT 계정 회수는 terra-server 몫 → 탈퇴 전 각 기기 `unlink` 호출 순서 제안. **이관훈님과 별도 합의 필요** |
| C7 | 버전 정보 | `package_info_plus`로 현재 버전, 최신 버전 비교는 스토어 API 없이 **고정 최소 버전(원격 설정)** 필요 → 1차는 "스토어 열기"만, 최신 판정은 후속 |

## 4. 순서 제안과 규모

1. 로그인(B) — 독립, 작음. 공용 필드 컴포넌트가 나머지의 기반.
2. 기기 예약·컨트롤 시트(A) — 가장 큼. 기존 편집기 재사용으로 절반은 이식.
3. 마이페이지(C) — 화면 6장, 서버 의존 2건(C4 dispatcher, C6 탈퇴 API)은 나머지와 분리해 뒤로.

각 단계는 Standard 트랙(메인 직접 구현), 커밋 단위·버전 bump·CHANGELOG 규칙 동일. 캡처 비교 표는 RESULTS 형식으로 `docs/design-audits/2026-09-17-…/`에 새로 만든다.

## 5. 새 세션 시작 프롬프트 (복사용)

```
/Users/baek/myProjects/tera-ai-flutter/.worktrees/redesign-20260915 에서 docs/superpowers/plans/2026-09-16-next-session-sheets-login-mypage.md 를 읽고 시작해.
Figma는 talk-to-figma 채널 <새 채널 id> 로 join. 순서는 문서 §4. §1-3·§2-3·§3-3의 "사용자 확인/결정 필요" 항목은 착수 전에 한 번에 물어봐.
실기기 명령 전송 금지, 운영 DB 변경 금지, 커밋은 단위별 자동, push는 내가 말할 때.
```
