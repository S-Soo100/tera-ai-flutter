# 비바나트 패치노트

이 파일은 비바나트 앱의 릴리스별 변경 사항을 한글로 기록하는 단일 패치노트입니다.

- 최신 버전을 위에 기록합니다.
- 제목은 `## [버전+빌드] - YYYY-MM-DD` 형식을 사용합니다.
- `추가`, `변경`, `수정`, `제거`, `보안` 중 필요한 분류만 작성합니다.
- 앱 동작에 반영되지 않은 계획은 완료된 변경으로 기록하지 않습니다.

## [0.104.0+208] - 2026-09-18

### 수정

- 사육장 기기를 BLE로 등록하면 Wi-Fi만 연결되고 서버에 기기가 생기지 않던 문제를 고쳤습니다. 앱이 기기 이름과 로그인 토큰을 기기에 다시 전달해 기기가 직접 서버에 등록합니다.

### 변경

- 사육장 등록 시 삭제했던 기기나 다른 계정의 기기도 바로 재등록되도록 등록 초기화 명령을 먼저 보냅니다. 이 명령을 모르는 구 펌웨어에서도 등록은 그대로 진행됩니다.
- Wi-Fi 연결 뒤 서버 등록을 확인한 다음에만 "기기 등록 완료"를 표시합니다. 30초 안에 확인되지 않으면 실패 대신 "등록을 확인하지 못했어요" 안내를 보여 줍니다.
- 새 사육장 기기는 `사육장 1`부터 쓰지 않는 번호로 기본 이름을 붙입니다.
- 카메라 등록은 펌웨어 지원 전까지 기존처럼 Wi-Fi만 연결합니다.

## [0.103.2+207] - 2026-09-15

### 변경

- Android FCM 작업의 운영 상태, 보안 처리, 외부 인계와 남은 단계를 한 번에 재개할 수 있는 체크포인트 문서를 추가했습니다.
- 이관훈님과 petcam-lab 담당자에게 각각 전달할 FCM 연동 Slack 스크립트를 분리해 추가했습니다.
- Android FCM 저장소 마이그레이션과 알림 수집·발송 Edge Function을 운영 Supabase에 배포했습니다.
- 예약된 푸시 발송을 1분 주기로 처리하도록 Supabase Vault, `pg_net`, `pg_cron`을 연결했습니다. Android 실기기 수신 검증은 후속 단계로 남아 있습니다.

### 보안

- 발송 함수는 전용 Supabase secret API key를 `apikey` 헤더로만 검증하며, 키는 Vault에 보관하도록 변경했습니다.
- Firebase Admin 서비스 계정은 `FIREBASE_SERVICE_ACCOUNT_JSON` 이름의 Supabase Edge Function Secret에 등록하고 저장소·문서에는 값을 남기지 않았습니다.
- 외부 알림 생산자용 `PUSH_EVENT_INGEST_SECRET`을 Supabase Secret에 등록하고 안전한 전달을 위해 Vault에도 보관했습니다.
- 로그아웃 시 서버 알림 연결 해제에 이어 기기의 FCM 토큰도 삭제하도록 보강했습니다. 서버 연결 해제 실패 시에도 토큰 삭제를 시도합니다.
- 세션 만료 등 강제 로그아웃도 토큰을 삭제하며, 삭제 중 토큰 갱신이나 새 로그인으로 이전 토큰이 다시 등록되지 않도록 처리 순서를 보호했습니다.
- 같은 프레임의 로그아웃·로그인이 하나의 계정 전환으로 합쳐져도 이전 FCM 토큰을 삭제한 후 새 계정 토큰을 등록하도록 보강했습니다.

## [0.103.1+206] - 2026-09-15

### 수정

- 로그아웃 중 앱 복귀나 토큰 갱신이 기기 알림 연결을 다시 활성화하지 않도록 처리 순서를 보호했습니다.
- 푸시 확인은 현재 계정의 해당 알림 한 건만 조회하며, 알림 원본이 잠시 조회되지 않은 경우 다음 수신·탭에서 다시 시도합니다.
- 알림 목록과 읽음 상태가 즉시 갱신되도록 Supabase Realtime publication 등록을 보완했습니다.
- 기존 iOS 14 앱의 의존성 호환성을 유지하도록 Firebase 패키지 버전을 조정했습니다. iOS 푸시·APNs는 활성화하지 않았습니다.

## [0.103.0+205] - 2026-09-15

### 추가

- Android FCM 알림 권한 안내와 재시도, 설치별 토큰 등록·갱신, 로그아웃 전 기기 연결 해제를 추가했습니다.
- 포그라운드 알림 표시, 백그라운드·종료 상태의 알림 탭 이동과 긴급·안전 전용 알림 채널을 추가했습니다.
- Supabase 실시간 알림 센터에 최신순 목록, 개별·전체 읽음, 오류 재시도와 프로필 미읽음 표시를 연결했습니다.
- 서버 이벤트 수집·중복 방지·예약 발송을 위한 알림 이벤트와 outbox 계약을 추가했습니다. 운영 푸시 활성화는 서버 배포·비밀 값 설정·Android 실기기 수신 검증 후 진행합니다.

### 보안

- 알림 조회·읽음·토큰 등록을 현재 계정으로 격리하고, 알림 탭은 허용된 내부 경로만 이동하도록 검증합니다.

## [0.102.1+204] - 2026-09-15

### 변경

- Android 앱을 Firebase 프로젝트 `vivanaut-app`에 연결했습니다.

## [0.102.0+203] - 2026-09-15

### 변경

- 영문 브랜드, Dart 패키지명, 플랫폼 식별자를 `vivanaut`으로 정정했습니다.

## [0.101.5+202] - 2026-09-15

### 변경

- 카메라 활동 영상을 시간대별로 끊지 않고, 현재 조회 중인 전체 피드 순서대로 플레이어에서 이어 볼 수 있게 했습니다.
- 플레이어가 재생목록 끝에 가까워지면 다음 영상 페이지를 미리 불러오도록 개선했습니다.
