# 비바나트 패치노트

## 0.138.0+353 — 2026-09-25

### 변경
- 기기 추가에서 블루투스로 기기에 연결하지 못한 경우를 비밀번호 오류와 구분합니다. 전에는 기기가 멀거나 검색이 끝나 연결되지 않아도 "네트워크 비밀번호를 다시 확인해 주세요"가 떠서 맞는 비밀번호를 계속 다시 입력하게 됐습니다. 이제 결과 화면에 "휴대폰을 가까이 두고, 카메라는 전원을 껐다 켠 뒤 3분 안에 다시 시도해 주세요"처럼 할 일을 보여 주고, 로그인 만료와 기기의 등록 거절도 따로 안내합니다.
- 기기 검색 화면에 "카메라는 전원을 껐다 켜면 3분 동안 검색돼요" 등 기기가 안 보일 때 할 일을, 네트워크 선택 화면에 "기기는 2.4GHz Wi-Fi에만 연결돼요"를 안내합니다.
- 사육장과 카메라를 함께 추가하면 카메라부터 연결합니다. 카메라는 켜진 뒤 3분만 검색되기 때문입니다.
- 이미 등록된 사육장을 다시 연결하려 하면, 새 기기로 등록돼 이름·그룹·예약·기록이 이어지지 않는다는 확인을 먼저 받습니다.
- "새 카메라로 등록"은 확인을 받은 뒤 진행하고, 새 등록이 성공하면 옛 카메라 등록을 해제합니다. 전에는 옛 카메라가 그룹 안에 오프라인 상태로 계속 남았습니다.
- 기기 관리 목록에서 기기마다 늘 "ON" 대신 실제 연결 상태(온라인·오프라인)를 보여 줍니다.
- 블루투스 권한을 "다시 묻지 않음"으로 거부했으면 [설정 열기] 버튼을, 블루투스가 꺼져 있으면 그 안내를 보여 줍니다.
- 카메라를 모두 지운 카메라 탭에서도 북마크한 영상으로 갈 수 있습니다.

### 수정
- "등록 상태 다시 확인"을 눌러도 확인할 수 없는 경우 아무 반응이 없던 문제를 고쳤습니다. 아직 확인되지 않았다고 알려 줍니다.
- 카메라 Wi-Fi 변경 뒤 끝내 연결되지 않았는데 제목이 "Wi-Fi 변경완료"로 나오던 문제를 고쳤습니다.
- 새 기기 이름 번호("사육장 N")를 매길 때 해제된 기기 이름까지 세어 지웠다 다시 붙일 때마다 번호가 커지던 문제를 고쳤습니다.
- 이미 도마뱀이 있는 사육 환경에 합류했는데도 "도마뱀을 등록해 주세요"를 권하던 문제를 고쳤습니다.
- 이미 지워졌거나 찾을 수 없는 기기를 삭제하려 할 때 "서버에서 아직 지원하지 않습니다"로 잘못 안내하던 문구를 고쳤습니다.

## 0.137.0+352 — 2026-09-25

### 변경
- 카메라 라이브가 연결되지 않을 때 원인별로 안내합니다. "카메라에서 영상이 오지 않아요", "영상이 자꾸 끊겨요", "카메라와 연결하지 못했어요", "카메라가 응답하지 않아요" 등으로 나누고, 카메라 전원을 껐다 켜기처럼 할 일을 함께 보여 줍니다. 꺼진 카메라는 마지막 신호 시각도 보여 줍니다.
- 첫 영상을 기다리는 한도를 30초에서 10초로 줄였습니다. 운영 데이터상 연결 뒤 첫 영상은 최대 5.6초 안에 옵니다.
- 서버에 이미 오프라인으로 표시된 카메라는 30~45초 동안 연결을 시도하지 않고 바로 오프라인으로 안내합니다. 카메라가 켜지면 자동으로 연결되고, "다시 연결"을 누르면 한 번은 실제로 시도합니다.
- 실패 화면에서 자동으로 다시 확인하는 동안에도 실패 사유와 "다시 연결" 버튼이 유지됩니다. 전에는 60초마다 "연결 중"으로 바뀌어 무한 반복처럼 보였습니다.
- 화면에 보이지 않는 라이브(다른 탭, 위를 덮은 화면, 넘긴 카메라)는 30초 뒤 연결을 끊고, 다시 보이면 연결합니다. 데이터와 배터리를 아끼고, 30초 안에 돌아오면 다시 연결하지 않고 바로 보입니다. 시청 기록도 화면에 보인 시간만 셉니다.
- 카메라 탭에서 카메라를 옆으로 넘길 때 절반이 아니라 넘기기가 끝난 뒤에 카메라가 바뀝니다.
- 영상이 멈췄을 때 알약 문구를 "영상이 잠시 멈췄어요"로 바꿨습니다.

### 수정
- 카메라 탭 드롭다운으로 카메라를 바꾸면 홈이 따라 바뀌지 않아 두 화면이 서로 다른 카메라를 동시에 연결하던 문제를 고쳤습니다.
- 카메라 목록을 다시 불러오다 한 번 실패하면 라이브 자리가 "오류" 카드로 바뀌던 문제를 고쳤습니다. 이제 이전 목록으로 계속 보여 줍니다.
- 카메라 화면 회전은 재부팅(약 20초) 뒤 적용되는데, 그 사이 회전 버튼을 다시 누르면 반대 값이 전송되던 문제를 고쳤습니다. 적용될 때까지 버튼이 잠깁니다.

## 0.136.0+351 — 2026-09-25

### 변경
- 분무 분사 시간을 5·10초에서 **3·6·9초**로 바꿨습니다(기본 3초). 서버가 6·9초를 한 번에 받기 전까지 6·9초는 앱이 3초를 2·3번 이어 보냅니다. 기기가 받았는지 확인한 뒤 분사가 끝나고 2초 쉬고 다음을 보냅니다. 도는 동안 타일과 시트에 "분사 중 2/3"이 보이고, 시트에서 남은 분사를 멈출 수 있습니다. 중간에 거절·무응답·중지가 나거나 앱이 백그라운드에서 늦게 깨어나면 남은 분사는 보내지 않고 "9초 중 3초만 분사됐어요"처럼 실제로 뿌린 시간을 알려 줍니다. 예약은 서버가 한 번에 실행해서 이어 붙일 수 없으므로 서버 지원 전까지 3초만 고를 수 있습니다. 전에 고른 5·10초 선택은 3초로 돌아갑니다.
- 제어 기록에서 이어 보낸 분무는 "분무 9초 작동"처럼 한 줄로 묶어 보여 줍니다.
- 기기가 8초 안에 응답하지 않으면 "실행되지 않았다"고 단정하지 않고 "응답이 늦어요, 잠시 뒤 확인해 주세요"로 안내합니다. 분무는 늦게 도착해 두 번 뿌려지지 않도록 30초 동안 다시 누를 수 없습니다.
- 예약 목록(예약 화면·제어 시트)의 줄마다 마지막 실행 결과를 보여 줍니다(예: "마지막 실행 09:15", "09:15 실행 실패(기기 응답 없음)"). 예약 시각 알림 문구는 실행을 단정하지 않고 결과는 예약 목록에서 보도록 바꿨습니다.
- 다른 제어의 확인을 기다리는 동안 눌리지 않는 타일은 흐리게 보입니다.
- 사육장 알림 프리팝업에서 "받지 않기"를 고른 뒤에는 예약 화면·팬 타이머가 OS 알림 권한 창을 다시 띄우지 않습니다.

### 수정
- 사육장 연결 상태를 앱을 켤 때 한 번 읽은 값이 아니라 선택한 기기의 실시간 값으로 판정합니다. 앱을 켤 때 꺼져 있던 사육장이 다시 켜져도 앱을 재시작하기 전까지 "기기 연결이 끊겼어요"와 제어 잠금이 풀리지 않던 문제, 사육장이 2대 이상이면 다른 기기의 상태로 판정하던 문제를 고쳤습니다.
- 제어 시트를 연 채로는 거절·미전달 안내와 분무 "실행 취소"가 시트 뒤에 가려져 보이지 않던 문제를 고쳤습니다. 안내가 시트 위에 뜨고, 분무 완료 토스트도 이제 표시됩니다.
- 켜진 팬에서 이미 선택된 시간 칩을 다시 누르면 같은 명령이 또 나가 기기가 "바쁨"으로 거절하거나 타이머가 처음부터 다시 돌던 문제를 고쳤습니다. 시간 변경이 실패하면 칩도 원래 값으로 돌아가고, 실패한 선택은 기억하지 않습니다.
- 냉각팬이 타이머 없이 켜져 있으면 "30분 뒤"가 선택된 것처럼 보이던 문제를 고쳤습니다. 냉각팬이 없는 보드에서는 예약 추가 목록에 냉각팬이 나오지 않습니다.
- 인터넷 없이 앱을 연 뒤 홈의 "다시 시도"를 눌러도 복구되지 않던 문제를 고쳤습니다.
- 로그아웃하면 이 폰에 걸어 둔 예약·팬 타이머 알림을 모두 내리고, 삭제한 기기의 예약 알림은 앱을 열 때 정리합니다. 전에는 로그아웃·삭제 뒤에도 매일 울렸습니다.
- LCD 문구는 기기가 꺼져 있으면 보내지 않고 이유를 알려 줍니다. 홈의 LCD 줄에는 이번에 보낸 문구를 보여 줍니다.
- 예약 목록을 불러오는 중이거나 실패했을 때 "예약 없음"처럼 보이던 문제를 고쳤습니다(로딩 표시·다시 시도 버튼). 오류는 원문 대신 짧은 한글로 안내합니다.

## 0.135.1+350 — 2026-09-25

### 수정
- 라이브 시청 기록(0.135.0)에서 앱을 잠깐 나갔다 바로 돌아오면, 이전 연결 정리가 끝나기 전의 "재생 중" 상태가 새 기록에 넘어가 영상을 보지 못했는데도 "첫 영상 0초"로 남던 문제를 고쳤습니다. 복귀한 기록은 항상 "연결 중"에서 시작합니다.
- 시청 기록의 경과 시간을 휴대폰 시계 대신 단조 증가 시계로 잽니다. 시청 중에 휴대폰 시간이 뒤로 보정되면 시간이 음수가 되어 그 기록이 저장되지 않던 문제를 막습니다.

## 0.135.0+349 — 2026-09-25

### 추가
- 카메라 라이브를 한 번 볼 때마다(화면을 연 순간부터 떠나거나 앱을 내릴 때까지) 요약 기록 1건을 운영 DB(`webrtc_view_logs`)에 남깁니다. 결국 영상을 봤는지, 첫 영상까지 걸린 시간, 연결 중·재생·정지·자동 복구·실패 화면에 머문 시간, 수동 다시 연결 횟수, 재연결 사유별 횟수가 담깁니다. 화면과 동작은 바뀌지 않습니다.
- 기존 연결 기록(`webrtc_connect_logs`)에 같은 시청 세션 id와 당시 카메라 펌웨어 버전을 붙여, 펌웨어 버전별로 품질을 비교할 수 있습니다.
- 운영자용 조회 view(`ops.live_views`·`ops.live_daily`·`ops.live_timeline`)를 추가했습니다(`supabase/migrations/20260925_webrtc_view_logs.sql`). 로그인 사용자에게 노출되지 않는 `ops` 스키마에 있으며, 운영 DB에 2026-09-25 적용을 마쳤습니다.

## 0.134.2+348 — 2026-09-25

### 변경
- Microsoft Clarity 세션 녹화를 껐습니다. 동의 절차와 화면 가리기(비밀번호 등) 정책을 현재 화면 구조에 맞춰 다시 정하기 전까지 앱 사용 화면을 외부로 수집하지 않습니다.

## 0.134.1+347 — 2026-09-24

### 수정
- 카메라 Wi-Fi 변경 결과의 서버 확인(0.133.16)을 더 엄격하게 고쳤습니다. 카메라가 비밀번호 오류 등으로 실패를 직접 알린 경우와 연결 명령이 전달되기 전에 블루투스가 끊긴 경우는 서버를 보지 않고 바로 실패로 표시합니다(이때는 카메라가 옛 Wi-Fi로 계속 신호를 보내 서버 확인이 잘못 성공으로 읽힐 수 있었습니다). 서버로 판정하는 경우도 확인을 시작한 뒤 새로 들어온 접속 신호만 인정하도록 바꿔, 휴대폰과 서버의 시계 차이나 이전 신호에 흔들리지 않습니다.
- 어젯밤 리포트에서 연 영상(가로 전용 화면)에서 북마크 뒤 "공유하기"를 누르면 캡션 작성 화면이 가로로 나오던 문제를 고쳤습니다. 이동 전에 세로로 풀고 돌아오면 다시 가로로 잠급니다.
- 공유 제안 시트가 떠 있는 동안 북마크가 지워지거나 계정이 바뀐 경우, "공유하기"를 눌러도 사라진 영상으로 글쓰기에 들어가지 않고 조용히 닫힙니다.

## 0.134.0+346 — 2026-09-24

### 추가
- 영상을 북마크에 저장하면 그 자리에서 "커뮤니티에도 공유할까요?"를 물어봅니다. 공유하기를 누르면 클립 선택 단계를 건너뛰고 바로 그 영상의 캡션 작성 화면으로 이동합니다. "다시 묻지 않기"를 켜면 이 기기에서는 더 묻지 않습니다. 커뮤니티 탭의 + 버튼으로 시작하는 기존 방법은 그대로입니다.

### 변경
- 커뮤니티 글쓰기에서 북마크가 없을 때의 안내 문구를 현재 북마크 아이콘 기준으로 고쳤습니다(옛 ★ 표현 제거).

## 0.133.16+345 — 2026-09-24

### 수정
- 사육장과 카메라를 함께 추가할 때 카메라가 "연결 실패"로 표시됐는데 실제로는 연결돼 있던 문제를 고쳤습니다. 이미 등록된 카메라의 Wi-Fi를 바꾸는 경우 카메라가 Wi-Fi에 붙자마자 재부팅하며 블루투스를 끊어 성공 응답이 앱에 오지 않는 일이 잦았고, 앱은 그 응답만 믿고 실패로 판정했습니다. 이제 블루투스 응답이 없거나 실패여도 바로 실패로 두지 않고 "연결 확인 중"(최대 1분 30초)으로 서버에서 카메라 접속을 확인한 뒤 연결 완료·실패를 판정합니다. 끝내 안 붙으면 그때 "실패한 기기 다시 연결"과 "새 카메라로 등록"이 나옵니다.

## 0.133.15+344 — 2026-09-24

### 변경
- 하이라이트 목록과 어젯밤 리포트에서 영상을 열면 이제 항상 처음부터 재생됩니다. 그동안은 서버가 계산한 "움직임 직전" 지점으로 건너뛰어 시작했는데, 움직임 감지가 불안정해 오히려 움직임을 놓친 것처럼 보이는 경우가 있어 사용자 결정으로 껐습니다. 카메라 탭 영상 목록은 원래부터 처음부터 재생됐습니다.

## 0.133.14+343 — 2026-09-24

### 수정
- 카메라 라이브 정지 원인 기록을 정확하게 고쳤습니다. 멈추기 직전에 손상된 데이터가 조금 들어오고 이후 완전히 끊긴 경우를 '데이터는 오는데 화면만 멈춤'으로 잘못 기록하던 것을, 최근 3초의 수신량 기준으로 판정하도록 바꿨습니다. 연결 동작은 바뀌지 않습니다.

## 0.133.13+342 — 2026-09-24

### 수정
- 휴대폰 인터넷이 끊겼다 돌아온 뒤 사육장 온도·습도가 계속 안 나오고 '기기 연결이 끊겼어요'가 뜨던 문제를 고쳤습니다. 사육장 기기는 정상인데 앱의 실시간 수신 연결만 되살아나지 않았던 것으로, 이제 망이 돌아오거나 앱으로 돌아올 때, 수신 오류가 날 때 앱이 스스로 다시 연결합니다. 12초 동안 값이 안 오면 서버의 최신값을 직접 확인해 보여 주고, 서버엔 새 값이 있는데 못 받은 경우 수신 연결을 다시 만듭니다. 카메라 목록과 제어 명령 상태도 같은 방식으로 복구됩니다(Galaxy S21+ 실기기 시험에서 발견).

## 0.133.12+341 — 2026-09-24

### 추가
- 카메라 라이브 연결 진단에 정지 원인 구분을 추가했습니다. 영상이 멈췄을 때 그동안 데이터가 전혀 오지 않았는지(카메라·네트워크 쪽), 데이터는 오는데 화면만 멈췄는지(앱·디코더 쪽)를 수신 바이트·손실 패킷 증가량과 함께 기록합니다. 연결·재연결 동작은 바뀌지 않습니다.

## 0.133.11+340 — 2026-09-24

### 수정
- 카메라 라이브: 휴대폰 인터넷이 끊겼을 때 '카메라가 오프라인이에요'로 잘못 안내하던 것을 '휴대폰이 인터넷에 연결되어 있지 않아요'로 고쳤습니다. 인터넷이 돌아오면 같은 Wi-Fi여도 다시 연결 버튼 없이 자동으로 다시 붙습니다(Galaxy S21+ 실기기 시험에서 발견).

## 0.133.10+339 — 2026-09-24

### 수정
- 카메라 라이브 코드 리뷰 지적 4건을 고쳤습니다. ① 카메라 탭·확대 화면처럼 나중에 연 라이브가 첫 Wi-Fi↔LTE 전환을 놓치던 문제 ② 앱을 잠깐 나갔다 돌아왔을 때 영상은 나오는데 화면이 '라이브 연결 중'에 멈출 수 있던 문제 ③ 백그라운드 중 망이 바뀐 뒤 복귀하면 멀쩡한 새 연결을 한 번 더 취소하던 문제 ④ 망 변경 후 영상이 멈춰 다시 연결한 경우를 연결 기록에 '화면 이탈'로 남기던 문제.

## 0.133.9+338 — 2026-09-24

### 수정
- '라이브 연결 진단 내보내기'를 마이페이지 앱 설정(화면 모드 아래)으로 옮겼습니다. 0.133.8에서 넣은 환경설정 화면은 앱 안에 들어가는 경로가 없어 사용자가 찾을 수 없었습니다. 행 설명은 한 줄에 들어가도록 '지원 문의용 연결 기록 · 영상·비밀번호 제외'로 줄였습니다.

## 0.133.8+337 — 2026-09-24

### 추가
- 환경설정에 '라이브 연결 진단 내보내기'를 추가했습니다. 최근 라이브 연결 기록(최대 600건, 앱을 완전히 종료하면 사라짐)을 텍스트 파일로 공유할 수 있습니다. 영상·비밀번호·계정 정보는 포함되지 않으며 자동으로 전송되지 않습니다.

## 0.133.7+336 — 2026-09-24

### 수정
- 홈: 사육장 제어기 상태를 아직 불러오지 못한 동안 '기기 연결이 끊겼어요'를 띄우던 것을 '사육장 제어기 연결을 확인하고 있어요'로 바꿨습니다. 확인될 때까지 제어가 잠기는 것은 전과 같습니다.

## 0.133.6+335 — 2026-09-24

### 변경
- 카메라 라이브 안내 문구를 정리했습니다. 연결 단계별 문구('설정 수신 중…'·'카메라 호출 중…' 등)를 '라이브 연결 중' 하나로 합쳤고, 자동 복구 중에는 '자동으로 다시 연결하고 있어요', 최종 실패는 '라이브에 연결하지 못했어요'로 표시합니다. 영상이 멈추면 마지막 장면 위에 '영상이 잠시 멈췄어요 · 자동 연결 중'을 띄웁니다. 카메라 오프라인·휴대폰 인터넷 없음·로그인 만료는 각각의 사유로 안내하고, 버튼은 '다시 연결'입니다.

## 0.133.5+334 — 2026-09-24

### 변경
- 카메라 라이브: 처음 열었을 때와 끊긴 직후 90초 동안은 실패 안내 대신 자동으로 다시 연결하는 상태를 유지합니다. 90초가 지나도 연결되지 않으면 실패 안내와 다시 연결 버튼을 보이고 1분마다 조용히 다시 시도합니다(화면 문구는 0.133.6에서 반영).

## 0.133.4+333 — 2026-09-24

### 변경
- 카메라 라이브 재생 감시를 1초 단위로 바꿨습니다. 영상이 5초 멈추면 연결을 유지한 채 '잠시 멈춤' 상태로 두고, 15초까지 스스로 돌아오지 않을 때만 다시 연결합니다(전에는 5초 간격 확인으로 정지 판단이 최대 20초 늦었습니다). 영상 상태를 읽지 못하는 동안은 연결을 끊지 않습니다. 재시도 간격은 영상이 30초 안정된 뒤에만 처음으로 돌아갑니다. 화면 문구는 0.133.6에서 반영됩니다.

## 0.133.3+332 — 2026-09-24

### 수정
- 카메라 라이브: 네트워크 신호가 잠깐 흔들리거나 1초 안에 되돌아오면 재연결하지 않습니다. 영상이 나오는 중에는 망 종류가 바뀌어도 연결을 먼저 유지합니다. 휴대폰이 인터넷에 연결되지 않았을 때는 카메라 문제로 표시하지 않고, 연결이 돌아오면 자동으로 다시 붙습니다. 다시 연결 요청이 겹쳐도 카메라 호출은 한 번만 나갑니다. 재시도 간격은 연결 직후에 초기화하지 않습니다.

## 0.133.2+331 — 2026-09-24

### 수정
- 카메라 라이브: 연결이 60초 안에 성사되지 않으면 끊고 다시 시도합니다(전에는 연결 단계에서 무한히 기다릴 수 있었습니다). 로그인 만료로 실패하면 같은 요청을 반복하지 않고 안내와 다시 연결 버튼만 둡니다.

## 0.133.1+330 — 2026-09-24

### 변경
- 카메라 라이브 내부 상태에 '재생 중 일시 정지'와 '자동 복구 중'을 추가했습니다(이 버전 단독으로는 화면 변화 없음).

## 0.133.0+329 — 2026-09-24

### 추가
- 카메라 라이브 연결 과정(연결 시작·응답·첫 화면·끊김·재연결 사유)을 앱 안에 최근 600건까지 기록합니다. 메모리에만 두며 앱을 완전히 종료하면 사라집니다. 내보내기는 환경설정에 따로 추가됩니다.

## 0.132.2+328 — 2026-09-23

### 변경
- 환경설정의 "화면 뒤집기(180°)" 항목을 항상 표시합니다. 전에는 기능 지원을 보고하지 않은 카메라(구 펌웨어)나 카메라가 없는 사육장에서는 항목이 숨겨졌습니다. 카메라가 없는 사육장에서는 "이 사육장에 연결된 카메라가 없어요."라는 안내와 함께 흐리게 보이고 눌리지 않습니다. 구 펌웨어 카메라는 켜도 화면이 뒤집히지 않을 수 있습니다.

## 0.132.1+327 — 2026-09-23

### 변경
- 카메라 라이브 확대 화면의 "화면 뒤집기(180°)" 버튼을 세로·가로 모두에서 항상 표시합니다. 전에는 기능 지원을 보고하지 않은 카메라(구 펌웨어)나 카메라 정보를 불러오는 중에는 버튼이 숨겨졌습니다. 카메라 정보를 불러오기 전에는 버튼이 흐리게 보이고 눌리지 않습니다. 구 펌웨어 카메라는 버튼을 눌러도 화면이 뒤집히지 않을 수 있습니다.

## 0.132.0+326 — 2026-09-23

### 변경
- 분무 분사 시간 선택지를 5초·10초 두 가지로 줄이고 기본값을 5초로 바꿨습니다(7초 제외). 제어 시트와 분무 예약 편집기 모두 적용됩니다. 전에 7초를 골라 둔 사육장은 5초로 돌아갑니다.

## 0.131.1+325 — 2026-09-23

### 수정
- 분무 명령이 실패하거나 기기가 거절하면 분무 타일이 최대 12초 동안 "작동 중"으로 보이고 "1회 분사 시작" 버튼이 잠겨 있던 문제를 고쳤습니다. 이제 실패가 확인되는 즉시 풀립니다.

## 0.131.0+324 — 2026-09-23

### 추가
- 분무 제어 시트에 "분사 시간" 선택(5초·7초·10초)을 추가했습니다. 기본값은 7초이고, 마지막으로 고른 시간을 사육장별로 기억합니다. 시간을 고르기만 해서는 분무하지 않으며, "1회 분사 시작"을 눌러야 실행됩니다.
- 분무 예약에서도 분사 시간을 5초·7초·10초 중에서 고를 수 있습니다. 새 예약 기본값은 7초이며, 예약 목록에 "12:00 7초"처럼 분사 시간을 표시합니다.

### 변경
- 분무 버튼 잠금 시간이 고정 5초에서 "분사 시간 + 2초"로 바뀌었습니다(10초 분무면 12초). 분사 중에 다시 눌러 기기가 거절하는 일을 막습니다.
- 기존 1·2·3초 분무 예약은 저장된 시간 그대로 유지됩니다. 수정 화면에서 새 시간을 고를 때만 바뀝니다.

### 참고
- 5·7·10초 분무는 서버(허용 값 1·2·3초)와 펌웨어(최대 5초)가 업데이트되어야 실제로 동작합니다. 그 전까지는 "분무 실행 실패" 안내가 뜹니다. 요청서: `docs/handoffs/2026-09-23-mist-duration-5-7-10-server-request.md`.

## 0.130.0+323 — 2026-09-23

### 추가
- 카메라 라이브 연결 결과를 운영 테이블(`webrtc_connect_logs`)에 기록합니다. 연결마다 성공·실패 여부, 실패한 단계, 단계별 소요 시간, 연결 경로 종류(직접/중계), 네트워크 종류, 서버의 카메라 재요청 횟수를 남기고, 재생이 끝나면 재생 시간을 남깁니다. 화면 변화는 없으며 기록에 실패해도 라이브에는 영향이 없습니다. 기록은 운영자만 볼 수 있습니다.

## 0.129.0+322 — 2026-09-22

### 변경
- 카메라 라이브가 첫 영상이 실제로 도착해야 LIVE로 표시됩니다. 연결 직후 검은 화면 대신 "영상 받는 중..."이 보입니다.
- 앱을 백그라운드로 보내면 라이브 연결을 닫고, 앱으로 돌아오면 바로 다시 연결합니다(예전엔 끊긴 것을 알아채고 최대 1분 넘게 기다렸습니다).
- Wi-Fi와 LTE가 바뀌면 라이브를 곧바로 다시 연결합니다.
- 카메라가 꺼져 있으면 자동 재연결을 멈추고, 카메라가 다시 켜지면 바로 연결합니다.
- 연결 경로 탐색을 서버 권장값(2초)만큼 기다려 느린 망에서 첫 연결 성공률을 높였습니다.

### 수정
- 재연결할 때 이전 연결의 정보가 새 연결에 섞여, 한 번 끊기면 계속 연결이 실패하던 문제를 고쳤습니다.
- 연결 뒤 30초간 영상이 오지 않거나, 보는 중 영상이 15초간 멈추면 자동으로 다시 연결합니다(멈춘 화면이 LIVE로 남던 문제).
- 연결 도중 새로 연결하게 되면 쓰지 않는 카메라 세션을 닫아, 남은 세션이 다음 연결을 방해하지 않게 했습니다.

## 0.128.8+321 — 2026-09-22

### 수정
- 상태를 보고하지 않는 구형 LED 보드에서 켜기/끄기를 누를 때마다 사육장 제어가 8초 동안 잠기고, 그동안 LED가 켜진 것처럼 보이던 문제를 고쳤습니다. 이제 기기가 명령을 받으면 바로 풀리고 "상태 모름" 표시를 유지합니다.
- 분무 실행 취소 대기(2초) 중에 다른 제어를 누르면 분무가 아무 안내 없이 실행되지 않던 문제를 고쳤습니다. 대기 중에는 다른 제어가 잠기고, 만약 겹치더라도 "분무 실행 실패" 안내가 뜹니다.

## 0.128.7+320 — 2026-09-22

### 변경
- 사육장 제어(환기팬·냉각팬·LED·히터·분무)를 누르면 스위치와 홈 타일이 바로 누른 상태로 바뀌고, 기기가 실제로 반영할 때까지 반짝이는 로딩이 표시됩니다. 예전에는 기기 보고가 올 때까지 2~3초 동안 원래 자리에 머물러 눌렸는지 알 수 없었습니다.
- 로딩 중에는 같은 사육장의 다른 제어를 누를 수 없습니다. 연달아 누르면 기기가 두 번째 명령을 거절하던 문제(환기팬 켜기의 약 22%)를 막습니다.

### 수정
- 기기가 명령을 거절하거나 응답하지 않으면 16초 뒤가 아니라 확인되는 즉시(보통 1초, 길어도 8초) 알리고, 스위치를 원래 상태로 되돌립니다. 기기가 이전 명령을 처리 중이라 거절한 경우는 "잠시 후 다시 시도" 안내가 따로 나옵니다.
- 서버가 "기기 무응답(no_ack)"으로 확정한 명령을 알리지 않던 문제를 고쳤습니다.
- 분무 완료 토스트는 기기가 명령을 받은 뒤에 표시합니다.

## 0.128.5+318 — 2026-09-22

### 변경
- 사육 환경·도마뱀 저장 때 "다른 곳에서 구성이 바뀌었다"는 서버 응답을 새 오류 코드(PT409)로도 알아듣습니다. 서버가 같은 상황에서 응답을 끝없이 미루던 문제를 고치는 서버 변경(`supabase/migrations/20260922_redesign_conflict_errcode.sql`)에 맞춘 준비이며, 서버 적용은 별도로 진행합니다.

## 0.128.4+317 — 2026-09-22

### 수정
- 기기 추가에서 사육장과 카메라를 함께 등록하면 사육 환경이 자동으로 묶이지 않고 "사육 환경을 묶지 못했어요"가 뜨던 문제를 고쳤습니다. "사육 환경 다시 묶기"도 매번 같은 오류로 실패했는데, 이제 새 사육 환경으로 정상적으로 묶입니다.
- 사육장만(또는 카메라만) 등록한 뒤 기존 사육 환경에 합류하면, 그 환경에 도마뱀이 있을 때 로딩이 끝나지 않던 문제를 고쳤습니다. 합류해도 도마뱀은 그 환경에 그대로 남습니다.
- 기기 등록·합류 직후 홈의 사육 환경·기기 목록이 옛 상태로 남던 문제를 고쳤습니다.
- 합류 확인 화면에 기기 이름이 블루투스 이름(terra-iot 등) 대신 등록한 이름(예: 사육장 5)으로 나옵니다.
- 묶기에 실패했을 때 "도마뱀을 등록해 주세요" 안내와 실패 문구가 함께 뜨던 문제를 고쳤습니다.

### 변경
- 사육 환경 저장이 20초 안에 응답하지 않으면 기다리지 않고 "서버 응답이 없습니다" 안내로 끝냅니다(버튼이 잠긴 채 멈추지 않음).

## 0.128.3+316 — 2026-09-21

### 수정
- 온습도 상세(주간)에서 그날 최저값이 그래프 바닥 눈금에 가까우면(예: 습도 40.7%, 바닥 40%) 최저값 숫자가 아래 요일 글자와 겹치던 문제를 고쳤습니다. 그래프 위쪽처럼 아래쪽에도 한 칸 여유를 두어 그래프가 조금 길어졌습니다.

## 0.128.2+315 — 2026-09-21

### 수정
- 안드로이드에서 온습도 상세 화면을 끝까지 내려도 마지막 부분(습도 그래프 아래 요일 등)이 하단 내비게이션 바에 가려지던 문제를 고쳤습니다. 일간·주간 모두 바 높이만큼 여백을 둡니다.

## 0.128.1+314 — 2026-09-21

### 수정
- 온습도 상세(일간) 그래프 위 제어 아이콘이 실제 조작 시각 자리에 놓입니다. 짧은 시간에 여러 번 조작하면 아이콘이 약 1시간 간격으로 줄줄이 밀려, 조작하지 않은 시간대에도 기록이 있는 것처럼 보였습니다. 이제 가까운 기록은 제자리에서 살짝 겹쳐 표시되고 색으로 구분됩니다.

## 0.128.0+313 — 2026-09-21

### 추가
- 커뮤니티에서 다른 사람 영상을 확대해 볼 때 화면 아래에 재생바와 재생 시간(지금 / 전체)이 나옵니다. 재생바를 누르거나 끌어서 원하는 위치로 옮길 수 있습니다. 크레캠 영상 재생 화면과 같은 재생바이고, 동그란 조절점은 만지는 동안에만 보입니다.

## 0.127.0+312 — 2026-09-21

### 추가
- 기기 추가 목록에서 이 폰으로 이미 등록한 기기에 "이미 등록된 기기"(카메라는 "이미 등록됨 · Wi-Fi만 변경")가 표시되고 목록 아래로 내려갑니다. 등록을 마친 기기도 몇 분간 블루투스 신호를 계속 보내서, 설치 직후 기기 추가를 다시 누르면 방금 설치한 기기가 새 기기처럼 또 보였습니다. 삭제한 기기는 표시되지 않습니다.
- 한계: 이 폰으로 등록한 기기만 알아봅니다. 다른 폰이나 앱을 다시 설치한 뒤에는 표시되지 않습니다.

## 0.126.1+311 — 2026-09-21

### 수정
- 사육장을 새로 등록하면서 이미 등록한 카메라의 Wi-Fi만 다시 잡은 경우, 결과 화면에 "나중에 하기" 버튼만 남던 문제를 고쳤습니다. 이제 새로 등록한 사육장을 기존 사육 환경에 연결하는 버튼이 나오고, 연결하면 도마뱀 등록으로 이어집니다. Wi-Fi만 바꾼 카메라는 지금처럼 자동으로 다른 사육 환경에 옮기지 않습니다.

## 0.126.0+310 — 2026-09-21

### 추가
- 기기 등록 결과 화면에 사육장이 등록되지 않은 이유가 나옵니다. "서버 등록을 지원하지 않는 펌웨어", "기기가 서버 등록에 실패했어요 (사유)", "기기에서 서버 등록 응답이 오지 않았어요" 중 하나입니다. 전에는 모두 "등록 확인 대기" 한 줄로만 나와 원인을 구분할 수 없었습니다.
- 개발용 로그에 등록 단계별 응답(UNPAIR·NAME·JWT·연결·서버 등록 결과)을 남깁니다. Wi-Fi 이름·비밀번호·로그인 토큰은 남기지 않습니다.

### 변경
- 사육장이 Wi-Fi에는 연결됐지만 등록이 확인되지 않으면 "실패한 기기 다시 연결"로 다시 등록할 수 있습니다. 사육장은 다시 등록해도 같은 기기로 잡혀 목록에 늘어나지 않습니다. 카메라는 다시 등록하면 중복으로 늘어나서 지금처럼 "등록 상태 다시 확인"만 할 수 있습니다.

## 0.125.0+309 — 2026-09-21

### 수정
- 이미 등록한 카메라의 Wi-Fi를 다시 잡을 때 카메라가 하나 더 등록되던 문제를 고쳤습니다. 카메라는 등록 정보를 받을 때마다 새 기기 번호로 등록돼서, Wi-Fi를 바꿀 때마다 목록에 카메라가 늘어났습니다. 이제 앱이 이 폰으로 등록한 카메라를 기억해 두고, 같은 카메라면 등록 없이 Wi-Fi만 바꿉니다. 카메라 이름·사육 환경·영상 기록이 그대로 유지됩니다.
- Wi-Fi만 바꾼 뒤에는 결과 화면에서 카메라가 다시 켜져 새 Wi-Fi로 연결되는지 확인합니다. 90초 안에 연결되지 않으면(카메라를 초기화한 경우 등) "새 카메라로 등록" 버튼이 나옵니다.
- 한계: iOS는 폰마다 블루투스 주소가 달라, 다른 폰이나 앱을 다시 설치한 뒤에는 한 번 더 등록될 수 있습니다. 서버가 칩 고유 번호로 같은 카메라를 알아보게 되면(펌웨어 후속) 완전히 막힙니다. 이미 생긴 중복 카메라는 자동으로 정리되지 않습니다.

## 0.124.0+308 — 2026-09-21

### 변경
- 북마크 버튼이 북마크한 영상과 안 한 영상으로 모양이 갈립니다. 안 한 영상은 속이 빈 리본, 북마크한 영상은 꽉 찬 리본으로 나옵니다. 전에는 둘 다 꽉 찬 모양이라 구분이 어려워서 안 한 쪽을 연하게 그렸는데(0.120.0), 모양으로 갈리게 되면서 그 농도 차이는 없앴습니다. 연한 아이콘이 누를 수 없는 버튼처럼 보였기 때문입니다.
- 북마크 버튼을 눌러도 아이콘 크기가 변하지 않습니다. 전에는 북마크한 상태의 아이콘만 원본 크기가 달라 눌렀을 때 1.5배로 커졌습니다. 영상 재생 화면과 어젯밤 리포트에 함께 적용됩니다.

## 0.123.0+307 — 2026-09-21

### 보안
- 안드로이드 릴리즈 APK를 전용 서명 키로 서명하도록 바꿨습니다. 지금까지는 Flutter 템플릿 기본값대로 **디버그 키**로 서명하고 있었습니다. 디버그 키는 비밀번호가 공개돼 있어 누구나 APK를 변조해 다시 서명할 수 있고, 플레이 스토어에도 올릴 수 없습니다.
- ⚠️ **0.122.0+306까지 배포한 APK를 받은 분은 앱을 지우고 다시 설치해야 합니다.** 서명이 바뀌면 기존 설치본 위에 덮어쓸 수 없습니다. 로그인 정보와 기기에 저장된 메모가 함께 지워집니다.

### 변경
- 서명 키 설정이 없는 상태로 릴리즈를 빌드하면 빌드가 실패하고 안내가 나옵니다. 전에는 조용히 디버그 키로 서명돼서, 배포한 뒤에야 알 수 있었습니다. 디버그 빌드와 테스트는 키 없이 그대로 동작합니다.

## 0.122.0+306 — 2026-09-21

### 변경
- 영상 재생 화면의 메모 버튼이 메모가 있을 때와 없을 때로 구분됩니다. 메모를 남긴 영상은 아이콘이 꽉 찬 모양으로, 없는 영상은 지금처럼 외곽선으로 나옵니다. 툴팁도 "메모 추가"와 "메모 수정"으로 나뉩니다.

## 0.121.0+305 — 2026-09-21

### 추가
- 영상 재생 속도를 1배·1.2배·1.5배·2배 네 단계로 바꿀 수 있습니다. 전에는 1배와 2배만 오갔습니다. 배속 버튼을 누를 때마다 다음 단계로 넘어가고 2배 다음에는 1배로 돌아옵니다.

### 변경
- 1배속일 때 버튼에 글자로 "1×"가 나오던 것을 디자인 아이콘으로 바꿨습니다. 네 단계 모두 같은 모양의 아이콘을 씁니다.

## 0.120.0+304 — 2026-09-21

### 변경
- 북마크하지 않은 영상의 북마크 버튼을 50% 연하게 표시합니다. 북마크한 영상은 지금처럼 진하게 나옵니다. 두 상태가 모두 속이 찬 같은 모양이라 눌렀는지 아닌지 알아보기 어려웠습니다. 영상 재생 화면과 어젯밤 리포트에 함께 적용됩니다.

## 0.119.2+303 — 2026-09-21

### 수정
- 주간 활동 그래프의 분 단위 눈금(`60m` 등)이 "60"과 "m" 두 줄로 접혀 읽기 어렵던 문제를 고쳤습니다. 0.117.0에서 분 눈금을 넣으면서 생긴 문제입니다.

## 0.119.1+302 — 2026-09-21

### 수정
- 판정을 기다리는 영상이 많을 때 카메라 탭 목록 가운데가 비던 문제를 고쳤습니다. 직전 버전은 판정 전 영상을 맨 위에 한 페이지(60개)만 붙여서, 판정이 오래 밀린 카메라는 최근 60개 다음이 곧바로 몇 주 전 영상으로 건너뛰었습니다. 이제 판정 전 영상을 끝까지 스크롤해 본 뒤 판정된 영상으로 이어집니다. (실측: 한 카메라가 8월 4일 이후 판정이 멈춰 미판정 영상이 1187개였습니다.)

## 0.119.0+301 — 2026-09-21

### 변경
- 카메라 탭에서 아직 서버 판정을 기다리는 영상도 먼저 보여줍니다. 전에는 판정을 통과한 영상만 보여줬기 때문에 판정이 늦어지면 어제·오늘 찍힌 영상이 통째로 없는 것처럼 보였습니다. 판정이 끝나 기준에 못 미친 것으로 확정되면 그때 목록에서 빠집니다.

## 0.118.0+300 — 2026-09-21

### 변경
- 활동 시간을 "움직임이 있는 영상의 길이 합"으로 바꿨습니다. 전에는 영상 안에서 개체가 실제로 움직인 구간만 더해서, 1분짜리 영상에 3초만 잡히면 3초로 셌습니다. 이제 카메라 탭에 보이는 영상 한 편은 그 길이만큼 활동으로 셉니다 — 화면에 보이는 영상과 숫자가 같은 것을 가리킵니다. 표시되는 활동 시간이 전보다 크게 늘어납니다.
- 같은 이유로 활동 시간에 붙던 "추정" 표시가 사라집니다. 영상 길이는 추정이 아니라 아는 값입니다.

## 0.117.2+299 — 2026-09-21

### 수정
- 자동 로그인이 수시로 풀리던 문제를 고쳤습니다. 앱을 한동안 띄워 두었다가 돌아오면 앱이 만료된 인증 토큰을 그대로 서버에 보내고, 서버가 거절하면 곧장 로그아웃해 버렸습니다. 이제 토큰은 만료 전에 미리 갱신하고, 거절당해도 세션을 되살려 한 번 더 시도합니다. 로그아웃은 세션이 실제로 끝났을 때만 일어나며, 네트워크가 끊긴 것만으로는 로그아웃되지 않습니다.

## 0.117.1+298 — 2026-09-21

### 변경
- "모듈 화면 표시 문구" 입력칸을 열면 페어링 때 감지된 기기 이름(`terra-…`)이 미리 채워집니다. 전에 떠 있던 "예: 밥 6시" 예시 문구는 디자인 원본에 없어 없앴습니다. 이미 문구를 보낸 적이 있으면 전처럼 마지막 문구가 채워집니다.

## 0.117.0+297 — 2026-09-21

### 변경
- 주간 활동 그래프의 세로 축이 실제 활동량에 맞춰 줄어듭니다. 전에는 최소 3시간 축이 고정이라 하루 10분씩 움직인 주는 막대 7개가 전부 바닥에 붙은 점처럼 보였습니다. 이제 6분·12분·30분·1시간·1시간 30분·3시간·6시간·12시간·24시간 중 데이터를 담는 가장 작은 축을 쓰고, 1시간 30분 이하 축은 눈금을 분으로 표시합니다(예전에는 `0.5h`처럼 나왔습니다).

## 0.116.1+296 — 2026-09-21

### 수정
- 녹화 영상 재생 화면에서 북마크한 영상의 북마크 아이콘만 다른 버튼보다 1.5배 크게 그려지던 문제를 고쳤습니다. 북마크를 누르는 순간 아이콘이 확 커졌다가 해제하면 작아졌습니다.
- 같은 화면의 삭제(휴지통) 아이콘이 다른 버튼의 절반 크기로 작게 그려지던 문제를 고쳤습니다.

## 0.116.0+295 — 2026-09-19

### 변경
- 카메라 탭 하이라이트 카드가 아직 안 본 하이라이트가 있으면 "새 하이라이트"로 표시합니다. 하이라이트 화면의 "하이라이트가 도착했어요" 배너와 같은 기준이며, 그 하이라이트를 재생해 보면 원래대로 "업데이트 N일 전"으로 돌아갑니다. 전에는 카드가 "3일 전"인데 안에 들어가면 "도착했어요"가 떠서 서로 다른 말을 했습니다.

## 0.115.2+294 — 2026-09-19

### 수정
- 카메라 탭의 하이라이트·북마크 카드에서 "업데이트 N일 전" 문구가 탭을 옮기거나 앱으로 돌아올 때 잠깐 회색 막대로 깜빡이던 문제를 고쳤습니다. 새로 불러오는 동안에도 이전 문구를 그대로 보여 줍니다.
- 하이라이트·북마크 목록 화면에 들어간 직후 목록 전체가 한 번 회색으로 바뀌었다 돌아오던(스크롤 위치도 처음으로 돌아가던) 문제를 고쳤습니다.
- 홈에서 기기 정보를 새로 불러오는 동안 온습도 카드가 잠깐 사라져 아래 제어 버튼들이 위로 튀었다 돌아오던 문제를 고쳤습니다.

## 0.115.1+293 — 2026-09-19

### 수정
- 카메라가 연결돼 있으면 홈 화면 전체가 약 15초마다(카메라 대수만큼 더 자주) 한 번씩 회색으로 깜빡이던 문제를 고쳤습니다. 카메라가 보내는 생존 신호를 앱이 "카메라 목록이 바뀌었다"로 받아들여 홈을 통째로 다시 불러오던 것이 원인입니다. 이제 생존 신호만 바뀐 갱신은 무시하고, 실제로 목록이 바뀔 때도 홈은 이전 화면을 유지한 채 조용히 갱신합니다(라이브 영상·스크롤 위치가 끊기지 않습니다). 같은 원인으로 어젯밤 리포트 등이 15초마다 서버를 다시 조회하던 것도 함께 멈췄습니다.

## 0.115.0+292 — 2026-09-19

### 변경
- 카메라 탭 전체 영상 목록이 **움직임이 뚜렷한 영상(하이라이트 기준 통과분)만** 보여줍니다. 빈 화면·미세한 움직임 영상은 목록과 플레이어 넘김에서 빠집니다. 북마크한 영상과 활동시간 집계는 그대로입니다.
- 하이라이트는 **밤(20:00~다음 날 08:00) 촬영분만**, **촬영 다음다음 날 아침 8시**에 공개됩니다(예: 8/16 밤 → 8/18 08:00). 공개된 최신 밤에는 도착 배너가 뜨고, 하이라이트 카드 "업데이트 N일 전"은 공개 시각 기준입니다.
- 영상이 없을 때 안내 문구를 "움직임이 뚜렷한 영상이 없어요"로 바꿨습니다.

## 0.114.5+291 — 2026-09-19

### 수정
- 카메라 탭 하이라이트 카드가 하이라이트가 있어도 항상 "업데이트 없음"으로 표시되던 것을, 마지막 하이라이트가 올라온 날 기준 "업데이트 오늘 / 어제 / N일 전"으로 바꿨습니다. 하이라이트를 다 본 뒤에도 마지막 날짜를 알 수 있습니다. 서버가 아직 공개 시각을 주지 않아, 마지막 하이라이트가 촬영된 밤의 다음 날을 올라온 날로 계산합니다(서버가 공개 시각을 주면 그 값을 씁니다). 최근 30일 안에 하이라이트가 없으면 전처럼 "업데이트 정보 없음"입니다.

## 0.114.4+290 — 2026-09-19

### 수정
- Android 12 이상에서 앱을 켜면 스플래시가 두 번(큰 도마뱀 심볼 → 작은 vivanaut 워드마크) 나오던 문제를 고쳤습니다. Android 12+ 시작 화면에 옛 terra.ai 심볼이 따로 지정돼 있었는데, 이제 iOS·Android 모든 버전과 앱 안 스플래시가 같은 vivanaut 워드마크를 같은 크기로 보여 한 화면으로 이어집니다.

### 변경
- 스플래시 워드마크 폭을 200에서 190으로 줄였습니다. Android 12+ 시작 화면은 원형 영역(지름 192dp)만 보여 주므로, 잘리지 않는 최대 폭에 모든 플랫폼을 맞췄습니다. 로그인 화면 로고 크기는 그대로입니다.

## 0.114.3+289 — 2026-09-19

### 변경
- 홈 '기기 설정'의 첫 줄 이름을 "일정 설정"에서 "기기 예약 설정"으로 바꿨습니다. 눌렀을 때 열리는 화면 제목, Figma 원본과 이름이 같아집니다.

## 0.114.2+288 — 2026-09-19

### 변경
- 홈 기기 제어 시트(환기팬·분무·냉각팬·LED)를 열어도 뒤 화면이 어두워지지 않습니다. Figma 원본처럼 시트 위쪽 그림자로만 구분하며, 시트 바깥을 누르면 닫히는 동작은 그대로입니다.
- 하이라이트·북마크 화면 오른쪽 위 달력 아이콘을 Figma 원본(날짜 점이 있는 달력, 회색 #3C3C3C)으로 바꿨습니다. 카메라 탭 '기간 설정' 버튼의 달력은 원본과 같아 그대로입니다.

## 0.114.1+287 — 2026-09-18

### 변경
- 알림 설정의 기기 알림 꺼짐 안내 버튼 문구를 "알림 허용" 하나로 통일했습니다. Android는 한 번 거절한 뒤에도 시스템 팝업을 다시 띄울 수 있어 "설정 열기"라고 쓰면 실제로는 팝업이 뜨는 경우가 있었습니다. 누르면 팝업을 띄울 수 있으면 띄우고, 막혀 있으면 앱 설정을 엽니다.

## 0.114.0+286 — 2026-09-18

### 추가
- 마이페이지 > 알림에서 기기 알림 권한이 꺼져 있으면 맨 위에 "기기 알림이 꺼져 있어요" 안내와 버튼(아직 묻지 않았으면 "알림 허용", 거절했으면 "설정 열기")을 보여줍니다. 앱 안 토글이 켜져 있어도 알림이 오지 않는 이유를 알 수 있게 하기 위함입니다.
- 그동안 기능 설정(하이라이트·댓글·좋아요) 토글은 흐리게 표시하고 조작을 막습니다. 저장된 선택은 유지되어, 기기 설정에서 허용하고 돌아오면 안내가 사라지고 토글이 바로 살아납니다. 수신 동의 관리는 동의 기록이라 계속 조작할 수 있습니다. iOS는 푸시 발송 전이라 안내를 띄우지 않습니다.

## 0.113.0+285 — 2026-09-18

### 추가
- 사육장 기기를 등록한 직후 "사육장 알림을 켜시겠습니까?"(예약 작동 결과·사육장 이상 상황 알림)를 묻습니다. 카메라나 커뮤니티를 쓰지 않는 사용자도 사육장 푸시를 받을 수 있게 하기 위한 시점입니다. 시스템 알림 권한이 아직 없을 때만 한 번 묻고, "알림 받기"는 시스템 권한을 요청하며 "알림 받지 않기"는 팝업만 닫습니다. 이미 허용했거나 iOS에서는 묻지 않습니다.

## 0.112.0+284 — 2026-09-18

### 추가
- 알림 권한을 필요한 순간에 묻는 안내창 2종을 Figma(권한 요청 1179:4464)대로 추가했습니다. 카메라를 연결하고 영상이 1개 이상 쌓인 뒤 카메라 탭에 들어오면 하이라이트 영상 알림을, 커뮤니티에 게시물을 올린 뒤에는 댓글·좋아요 알림을 각각 한 번 묻습니다.
- "알림 받기"는 해당 알림을 켜고, 시스템 알림 권한을 아직 묻지 않았으면 시스템 팝업을 띄웁니다. 예전에 거절했으면 다시 요청하고, 막혀 있으면 앱 알림 설정으로 안내합니다. 이미 허용했으면 바로 켜집니다.
- "알림 받지 않기"는 해당 알림을 끄고, 시스템 알림이 켜져 있으면 "수신 거부 완료" 안내(마이페이지 > 알림에서 재설정)를 보여줍니다. iOS는 푸시 발송 전이라 시스템 팝업 없이 알림 설정만 켜고 끕니다.

### 제거
- 로그인 직후 한꺼번에 알림을 묻던 하단 시트를 없앴습니다.

## 0.111.7+283 — 2026-09-18

### 수정
- 마이페이지 알림 설정의 설명 문구가 좁은 화면에서 어절 중간("받는 알/림")에서 줄바꿈되던 것을, 한 줄을 유지하고 공간이 모자랄 때만 글씨를 줄이도록 바꿨습니다. 마케팅 동의 행의 동의 일자·설명 두 줄 구성은 그대로입니다.

## 0.111.6+282 — 2026-09-18

### 변경
- 앱 시작 화면(스플래시)의 옛 "terra ai" 로고를 vivanaut 워드마크로 바꿨습니다. 네이티브 시작 화면과 앱 안 스플래시가 같은 로고·크기(로그인 화면과 같은 폭)로 이어집니다. Android 12 이상은 원형 아이콘 규격이라 심볼만 표시합니다.

## 0.111.5+281 — 2026-09-18

### 수정
- 홈 온습도 카드의 "최고/최저" 줄이 좁은 화면에서 두 줄로 넘어가지 않도록, 공간이 모자랄 때만 글씨를 줄여 한 줄로 표시합니다.

## 0.111.4+280 — 2026-09-18

### 수정
- Figma 재설계 작업(0.105~0.111)을 main에 병합했습니다. 그동안 main 빌드에는 재설계 이전 화면이 나오고 있었습니다.
- 사육장을 BLE로 등록할 때 저장된 서버 등록 정보를 먼저 지우는 명령을 보냅니다. 삭제했던 기기나 다른 계정의 기기도 바로 다시 등록되고, 이 명령을 모르는 구 펌웨어에서도 등록은 그대로 진행됩니다. 카메라에는 보내지 않습니다.
- 기기가 JWT 수신 시작을 확인하지 않으면 Wi-Fi 연결을 시작하지 않고 실패로 안내합니다. 등록 없이 Wi-Fi만 붙는 상태를 막습니다.
- 기기가 서버 등록 실패(`PAIR_FAIL`)를 알리면 바로 등록 대기로 넘어가 다시 확인할 수 있습니다.
- 기기에 넘기는 로그인 토큰을 등록 직전에 새로 받아, 만료된 토큰 때문에 등록이 거절되지 않게 했습니다.

## 0.111.3+279 — 2026-09-16

### 수정
- 분무 "1회 분사 시작" 실행 취소 창: 시트를 닫아도 예정된 분무가 그대로 나가고, 스낵바를 스와이프하거나 다른 안내가 겹쳐도 2초 창이 유지됩니다(취소는 "실행 취소" 버튼만).
- LED 상태를 모르는 구 펌웨어 기기에서 제어 시트가 스위치 대신 켜기/끄기 버튼을 보여줍니다(끄기 명령을 보낼 수 있게).
- 제어 시트의 전원 스위치·시간 칩을 명령 왕복 중 연타해도 한 번만 보냅니다.
- 자동 로그인을 끈 채 앱을 다시 열 때와 회원 탈퇴 시 로그아웃이 푸시 기기 정리 경로를 거칩니다.
- 로그인 화면에서 한 칸을 고치면 그 칸의 오류만 사라집니다(다른 칸 오류 유지).
- 비밀번호 변경에서 새 비밀번호가 서버에 거절된 경우를 "현재 비밀번호 불일치"로 잘못 안내하지 않습니다.
- 명령 전송·설정 저장 뒤 화면이 닫혀 있을 때 생길 수 있던 내부 오류를 막았습니다.

## 0.111.2+278 — 2026-09-16

### 수정
- 홈 기기 제어 시트의 위 모서리를 원본대로 24pt 둥글게 했습니다.

## 0.111.1+277 — 2026-09-16

### 수정
- 홈 기기 제어 시트가 하단 탭바 위에 떠서 탭바가 보이던 문제를 고쳤습니다. 시트가 화면 바닥부터 올라옵니다(시뮬레이터 확인).
- 알림 설정의 부제가 실기기 서체 폭에서 "…"로 잘리던 것을 두 줄 줄바꿈으로 바꿨습니다. 마케팅 수신 동의 행은 동의 전에도 원본대로 12pt 설명을 씁니다.

## 0.111.0+276 — 2026-09-16

### 변경
- 마이 페이지(`/profile`)를 Figma 재설계대로 다시 만들었습니다. 커뮤니티 설정(프로필 카드·차단한 사용자 N명)과 앱 설정(알림·내 계정·버전 정보) 아래에 받은 알림(미읽음 점)·화면 모드·디자인 랩(개발 빌드) 행이 붙습니다. 로그아웃은 내 계정 화면의 빨간 버튼과 확인 모달로 옮겼습니다.
- 차단한 사용자 목록을 카드형으로 바꾸고, 카드를 누르면 "차단을 해제하시겠습니까?" 확인 뒤 해제하며 "차단 해제 완료" 토스트를 보여줍니다.

### 추가
- 커뮤니티 프로필 화면(`/profile/community`): 프로필 사진, 닉네임(10자 카운터, 중복 안내), 사육 경험 3택(입문자·중급자·숙련자), "커뮤니티에 내 사육 경험 비공개" 체크, 변경이 있을 때만 켜지는 저장 버튼. 비공개 값은 서버 컬럼(`experience_hidden`)이 생긴 뒤 저장됩니다(검토용 SQL `supabase/drafts/20260916_user_profiles_experience_hidden.sql`).
- 알림 설정 화면(`/profile/notifications`): 하이라이트 영상·커뮤니티 댓글·커뮤니티 좋아요, 비바나트 소식·마케팅 수신 동의(동의 일자 표기). 값은 아직 이 기기에만 저장되며 서버 발송을 막지는 못합니다(서버 요청서 `docs/handoffs/2026-09-16-mypage-server-requests.md`).
- 내 계정(`/profile/account`)·비밀번호 변경(`/profile/account/password`, 현재 비밀번호 확인 후 변경, 영문+숫자+특수문자 6~12자 규칙)·회원 탈퇴(`/profile/account/withdraw`) 화면. 회원 탈퇴 서버 함수는 아직 없어 "아직 준비되지 않았습니다"로 안내합니다.
- 공용 확인 모달 `showVivaModal`(345×144, 버튼 1개/2개).

## 0.110.0+275 — 2026-09-16

### 변경
- 홈 제어 타일(환기팬·분무·냉각팬·LED)을 누르면 Figma 재설계의 기기 제어 시트가 열립니다. 상단 [즉시 작동 | 예약 작동] 구분, "전원 · 켜짐/꺼짐" 행의 스위치, 환기팬 작동 시간(10분/30분/1시간/2시간/계속)·냉각팬 종료(30분/1시간/2시간 뒤 + 아이스팩 안내)·LED 밝기 행으로 구성됩니다. 칩·슬라이더는 선택만 하고, 전원 스위치를 켜야 그 값으로 명령을 보냅니다(끄기는 즉시 끄기 명령). 이미 켜진 기기에서 칩을 바꾸면 그 값으로 다시 켭니다. 환기팬 원탭·꾹 누르기 방식과 LED 켜기/끄기 시트는 이 시트로 대체됐습니다.
- 분무는 타일 즉시 분사 대신 시트의 "1회 분사 시작" 버튼으로 실행하며, 2초 동안 스낵바에서 "실행 취소"할 수 있습니다. 실행·취소·실패는 토스트("분무가 실행되었습니다" 등)로 알립니다.
- 예약 작동 탭에서 그 기기의 예약 목록을 보고 켜고 끌 수 있으며, "새 예약 추가"나 예약 줄을 누르면 시트 안에서 바로 편집하고 "예약 저장"합니다. 예약이 없으면 편집기가 바로 열립니다. 삭제는 예약 설정 화면에서 합니다.
- 팬 타이머 진행은 홈 상단 칩 대신 타일 부제에 "켜짐 · 30m 56s 뒤 꺼짐"처럼 표시합니다. 분무 진행 중 타일 상태는 "작동 중"입니다.

### 제거
- 홈 상단의 팬 타이머 진행 칩을 없앴습니다(타일 부제로 이동).

## 0.109.1+274 — 2026-09-16

### 변경
- 예약 편집기의 분 화살표가 5분이 아니라 10분 단위로 움직입니다(디자이너 메모 "시각 설정 분: 10분 단위 ±"). 10분 눈금 밖의 기존 예약 값은 화살표 한 번에 가까운 눈금으로 들어갑니다.
- 예약 편집 폼 본문을 `ScheduleEditorBody`로 분리해 예약 설정 화면과 홈 제어 시트의 예약 탭이 같은 폼을 쓰도록 준비했습니다. 화면 동작은 그대로입니다.

## 0.109.0+273 — 2026-09-16

### 변경
- 로그인 화면을 Figma 재설계(`Login` 1133:5712 계열)대로 다시 만들었습니다. vivanaut 워드마크 로고, "아이디"·"비밀번호" 필드(65pt·오류 시 빨간 테두리와 안내 문구), "자동 로그인" 체크, 두 칸을 모두 채워야 켜지는 하단 고정 "로그인" 버튼(키보드가 열리면 키보드 위로 이동)으로 구성됩니다. 회원가입 링크는 버튼 아래 텍스트로 유지했습니다.
- 자동 로그인을 끄고 로그인하면 다음에 앱을 열 때 세션을 끊고 로그인 화면을 다시 보여줍니다(끈 적이 없으면 기존처럼 유지).
- 로그인 검증은 이메일 형식과 비밀번호 6자 이상만 막습니다. "영문+숫자+특수문자 6자~12자" 규칙 문구는 힌트와 짧은 비밀번호 안내에만 쓰고, 기존 계정이 잠기지 않도록 로그인에서 강제하지 않습니다. 서버 인증 실패는 비밀번호 칸 아래 한글 안내로 보여줍니다.

### 추가
- 공용 입력 필드 `VivaTextField`(라벨·힌트·오류·카운터·비밀번호 눈 아이콘)와 체크 행 `VivaCheckRow`를 추가했습니다. 개체 폼·그룹 이름 입력도 이후 이 컴포넌트로 통일할 예정입니다.

## 0.108.7+272 — 2026-09-16

### 수정
- 개체 관리의 ⋮ 메뉴에서 "개체 정보 수정"이 두 줄로 잘리던 문제를 고쳤습니다. 메뉴 폭을 고정하지 않고 최소 140에서 글자에 맞춰 넓어지게 했습니다.

## 0.108.6+271 — 2026-09-16

### 수정
- 그룹 편집기에서 소속 개체를 누르거나 기기 관리에서 그룹 없는 개체를 누르면 그 뒤로 뒤로가기가 전부 먹통이 되던 문제를 고쳤습니다. 루트 화면에서 탭 안의 개체 경로를 열어 GoRouter 페이지 키가 겹치고, 그 예외가 내비게이터를 잠갔던 것이 원인입니다. 루트 화면용 개체 상세·수정 경로(`/pets/:id`, `/pets/:id/edit`)를 따로 두고 그쪽으로 엽니다. 마이크레 탭 안의 개체 화면은 그대로입니다.

## 0.108.5+270 — 2026-09-16

### 추가
- 가드 스킵 푸시 `device.action.skipped`를 앱이 받을 수 있게 열었습니다(수신 검증기·알림 종류·문구 마이그레이션). 문구는 "예약 동작을 건너뛰었어요"이며 온도/습도 가드 값을 함께 보여줍니다. 문구 마이그레이션과 수신 함수는 2026-09-16 운영에 배포했습니다.

## 0.108.4+269 — 2026-09-16

### 제거
- 구 사육장 탭(스마트 케이지)의 히터 타일을 없앴습니다. 두 보드 모두 펌웨어에 히터가 없어 명령이 항상 `unknown_action`으로 실패합니다(이관훈님 9/16 회신 §4.1). 홈·예약에서는 이미 숨겨져 있었습니다.

## 0.108.3+268 — 2026-09-16

### 수정
- 온습도 상세의 일평균이 서버가 새로 배포한 지표별 유효 표본 수 컬럼(`t_a_count`/`h_a_count`)을 읽도록 고쳤습니다. 이전 이름(`*_valid_count`)만 읽어 평균이 계속 `--`로 나왔습니다. 2026-09-15 이전 버킷은 값이 없어 그대로 `--`입니다(회신 §3.1).

## 0.108.2+267 — 2026-09-16

### 제거
- LED 시트의 "작동 시간" 칩(30분~3시간/계속)과 오늘 추가했던 LED 타이머 진행 칩·종료 알림을 없앴습니다. 펌웨어가 `led_on`의 `duration_ms`를 읽지 않고 그냥 켠 뒤 `ok`를 보내, 시간이 지나도 꺼지지 않는데 앱도 서버도 알 수 없기 때문입니다(회신 §1.4, 사용자 A안 확정). LED 켜기는 밝기만 보냅니다.

## 0.108.1+266 — 2026-09-16

### 변경
- 기기 관리의 "이 기기 삭제"는 terra-server의 등록 해제 API(`POST /devices|cameras/{id}/unlink`)를 호출합니다. 서버가 해제 시각(`unlinked_at`)만 기록하고 과거 영상·온습도 기록·소유권은 보존한다는 9/15 회신 §2.5 계약을 전제로 미리 구현했으며, API 미배포(404/405)면 "서버 미지원" 안내만 띄우고 직접 삭제로 우회하지 않습니다.
- 해제된 기기(`unlinked_at` 있음)는 기기 관리·홈 기기 목록·카메라 목록에서 보이지 않습니다. 컬럼이 아직 없는 서버에서는 기존과 동일하게 전부 보입니다.
- 검토용 SQL 초안(`supabase/drafts/20260915_redesign_groups.sql`)에 `unlinked_at` 컬럼과 "해제된 기기는 그룹 구성원·이름 중복 검사에서 제외" 규칙을 추가하고 격리 DB에서 재검증했습니다. 운영 DB에는 적용하지 않았습니다.

### 제거
- 구 카메라 상세 화면의 카메라 삭제 버튼을 없앴습니다. 이 버튼은 카메라 행을 실제로 지워 영상 기록까지 함께 삭제됐습니다(회신 §2.1).

### 변경 (서버 계약)
- 푸시 이벤트 수신 검증기(`notification-ingest`)를 9/16 답신 내용대로 바꿨습니다. `execution_source`는 `schedule`만 받고, 성공/실패 판정은 `outcome`(succeeded/failed), 펌웨어 원문은 `result`(ok/busy/no_ack…)로 분리하며, `device_id`(UUID) 옆에 `device_key`(MQTT client id)를 선택 필드로 받습니다. 이전 형식(`result`에 succeeded/failed, `timer` 소스)은 400으로 거부합니다.
- 예약 동작 알림 문구에 기기 이름과 동작(환기팬 켜기·LED 끄기 등)을 넣고, 기기 무응답(`no_ack`/`expired`/`lost`)은 "다시 시도" 대신 "기기 연결 상태를 확인해 주세요"로, `busy`는 "다른 동작 중"으로 구분하는 마이그레이션(`20260916000000_notification_device_action_copy.sql`)을 추가했습니다. 2026-09-16 운영 DB에 적용했고 수신 함수(`notification-ingest` v7)도 함께 배포했습니다.

## 0.108.0+265 — 2026-09-16

### 추가
- LED 작동 시간(30분~3시간)을 걸고 켜면 홈 상단 진행 칩에 "LED N분 타이머 가동 중 (남은 시간)"이 팬 타이머와 같은 방식으로 표시되고, 만료 시각에 "LED 타이머 종료" 로컬 알림이 옵니다. 끄기나 '계속'으로 다시 켜면 칩과 알림이 함께 내려갑니다. 서버·펌웨어의 `led_on` + `duration_ms` 자동 꺼짐 지원을 전제로 미리 구현했으며(9/16 확인 요청서 §1), 미지원 회신 시 칩·알림도 함께 제거합니다.

## 0.107.53+264 — 2026-09-16

### 수정
- LCD 문구 화면을 탭바 없는 전체 화면으로 띄웁니다(원본과 동일).
- LED 밝기 슬라이더의 눈금 점을 없앴습니다(원본 트랙에는 눈금이 없음).

## 0.107.52+263 — 2026-09-16

### 변경
- 마이크레 일평균·주간 평균에 과거 영상 기반 추정 활동일도 추정값 그대로 포함하고, 그런 경우 평균 옆에 "추정"을 표시합니다. 이전에는 추정일을 빼고 영상 없는 날만 0으로 세어 추정 데이터만 있는 계정에서 평균이 0h 0m 또는 `--`로만 나왔습니다. 안내 문구도 "평균에도 추정값이 들어갑니다"로 바꿨습니다.

## 0.107.51+262 — 2026-09-16

### 추가
- 사육장이나 카메라 하나만 새로 연결했을 때, 반대 기기만 있는 그룹이 하나면 "연결한 사육장과 함께 사용할까요?" 확인 화면을 보여주고 "함께 사용하기"로 그 그룹에 바로 합류합니다. "따로 사용하기"는 이전처럼 그룹 없이 둡니다. 후보 그룹이 여럿이면 기존 "기존 기기와 연결" 경로를 씁니다.
- 개체를 그룹 선택 없이 등록했을 때 기기가 있는 그룹이 하나면 "도마뱀과 기기를 연결합니다" 확인 화면에서 "이 사육 환경에서 키우기"로 바로 배정할 수 있습니다. "나중에 하기"는 배정 없이 닫힙니다.

## 0.107.50+261 — 2026-09-16

### 추가
- LED 제어 시트에 "작동 시간" 칩(30분/1시간/2시간/3시간/계속)을 넣었습니다. 시간을 고르고 켜면 `led_on` 명령에 자동 꺼짐 시간(duration_ms)을 함께 보냅니다. 서버·펌웨어의 자동 꺼짐 지원은 확인 요청 중이며, 확인 전까지는 시간이 지나도 꺼지지 않을 수 있습니다.
- LED 예약 편집기에 밝기 행(기본 50%)을 넣었습니다. 켜기 예약에 밝기를 함께 저장하며, 서버가 예약 밝기를 반영하는지는 확인 요청 중입니다.

### 변경
- 냉각팬 예약은 시작 시각 + 작동 시간(30분/1시간/2시간)을 한 건(fan2_on + duration_ms)으로 저장합니다. 목록에는 "12:00~12:30"처럼 보이고, 기존 시작/종료 짝 예약은 그대로 표시·수정됩니다. 서버가 예약의 작동 시간을 실행에 반영하는지는 확인 요청 중입니다.

## 0.107.49+260 — 2026-09-16

### 변경
- 마이크레 일평균·주간 평균 활동 시간의 분모를 바꿨습니다. 카메라가 연결된 기간의 지난 날은 모두 관측 완료로 보고, 영상이 없는 날은 활동 0으로 평균에 넣습니다(움직임이 있을 때만 녹화되므로 영상 없음은 조용한 날로 봅니다). 진행 중인 오늘, 카메라 연결 전 날짜, 과거 추정 데이터만 있는 날은 종전대로 제외합니다. 서버가 수집 구간을 주기 시작하면 그 값을 우선합니다. 그동안 `--`로 남던 평균이 값으로 표시됩니다.

## 0.107.48+259 — 2026-09-16

### 추가
- LCD 문구 화면 상단에 원본 모듈 그림(디자이너 전달 x3 PNG)을 넣었습니다.
- 예약 편집기의 시·분 위/아래 화살표를 원본 아이콘으로 교체했습니다.

### 변경
- 홈 헤더의 사육장 드롭다운과 [+] 메뉴 그림자를 원본 값(#919497 30%, 흐림 20)에 맞췄습니다.

### 제거
- LCD 문구 화면의 "기본값 복원" 버튼을 없앴습니다(원본에 없음, 사용자 결정). 완료 버튼만 남고 위치는 원본과 같습니다.

## 0.107.47+258 — 2026-09-16

### 수정
- 영상 플레이어 가로 모드의 축소 버튼과 ±10초 이동 안내 칩에 원본 아이콘(zoom_in_map, fast_forward)을 연결했습니다. 되감기 칩은 같은 아이콘을 좌우 반전해 씁니다.
- 작은 세로 화면(320×568)에서 플레이어 하단 여백이 11pt 넘치던 문제를 화면 높이 비율로 줄여 바로잡았습니다. 기본 화면(393×852)의 위치는 그대로입니다.

## 0.107.46+257 — 2026-09-16

### 변경
- 기기 관리의 개체 탭에서 들어가는 구형 개체 상세 화면의 삭제 확인을 개체 관리와 같은 공통 확인창(이름 받침에 맞춘 "○○를 삭제하시겠습니까?" 문구, 검정 취소·빨간 삭제)으로 바꿨습니다. 삭제 동작과 체중·이벤트·사진 데이터 처리는 그대로입니다.

## 0.107.45+256 — 2026-09-16

### 변경
- 하이라이트 목록을 Figma 원본과 같은 구성으로 바꿨습니다. 각 묶음은 날짜 헤더(공개 배치의 실제 촬영 기간, 예: 2026. 8. 28 - 8. 31) 아래 카메라 홈과 같은 3열 썸네일 그리드로 이어지며, 전폭 대표 카드와 시각 표시는 없앴습니다. 도착 배너의 날짜도 같은 기간을 보여주고, 배치 정보가 없는 묶음은 이전처럼 "어젯밤/M월 d일 밤"으로 표시합니다.
- 배너 제목·날짜와 묶음 헤더의 행간을 원본 값으로 고정해 배너 높이가 8pt 커지던 문제를 바로잡았습니다. 재생 순서, 재생 시작점, 읽음 처리, 배너 닫기 동작은 그대로입니다.

## 0.107.44+255 — 2026-09-16

### 변경
- 기기 예약 설정 화면을 Figma 원본으로 바꿨습니다. 회색 바탕에 "기기 예약 설정" 헤더와 휴지통, 기기 아이콘·시각(예: 12:00~15:30)·요일·켜짐/꺼짐이 있는 흰 줄 목록(시작 시각 순), 하단 고정 "예약 추가" 버튼, 빈 목록 안내 상자로 구성됩니다. 같은 시작·종료 짝은 한 줄이며 스위치가 둘 다 바꿉니다.
- 예약 추가는 기기 선택(환기팬/분무/냉각팬/LED) 뒤 기기별 전체 화면 편집기로 들어갑니다. 환기팬·LED는 시작/종료, 냉각팬은 시작 + 30분/1시간/2시간 뒤 종료, 분무는 시작만 입력하며, 오전/오후·시·분 화살표와 월~일 요일 칩(안 고르면 매일)을 씁니다. 줄을 누르면 같은 편집기로 수정하고 아래 "예약 삭제"로 지울 수 있습니다.
- 휴지통을 누르면 삭제 모드가 되어 여러 예약을 체크한 뒤 "N개 항목 삭제"로 지우고, "예약을 삭제하시겠습니까?" 확인창(취소/삭제)을 거칩니다. 일부 실패 시 거기서 멈추고 목록을 다시 읽습니다.
- 편집기에서 스마트 조건(가드)은 다루지 않지만 기존에 걸린 조건은 그대로 유지되고 목록에 표시됩니다. 새 분무 예약은 홈 타일과 같은 3초로 저장됩니다.

### 제거
- 예약 화면 하단의 "가동 중 자동 정지·히터 타이머는 펌웨어 지원 대기" 각주를 화면에서 뺐습니다(문서로 이동).

## 0.107.43+254 — 2026-09-16

### 변경
- 환기팬·냉각팬 작동 시간 시트를 Figma 원본 모양(회색 시트, "작동 시간"/"종료" 라벨, 흰 44pt 알약 칩과 장치색 선택 칩, "계속" 80pt)으로 바꿨습니다. 냉각팬은 원본대로 "30분 뒤/1시간 뒤/2시간 뒤"만 제공하고 "계속"은 두지 않습니다. 시간 선택 후 "시작"으로 보내는 기존 동작은 그대로입니다.
- LED 시트의 밝기 조절을 원본과 같은 흰 행(퍼센트 + 노란 트랙 + 흰 알약 썸)으로 바꿨습니다. 켜기/끄기 버튼과 적용 시점은 그대로입니다.
- 원본의 즉시/예약 전환, 전원 스위치, LED 작동 시간은 송신 시점과 서버 계약이 확정되지 않아 이번에는 넣지 않았습니다.

## 0.107.42+253 — 2026-09-16

### 변경
- LCD 문구 설정을 바텀시트에서 Figma 원본과 같은 전체 화면("모듈 화면 표시 문구")으로 바꿨습니다. 안내문·입력칸(글자 수 N/20)·"완료" 버튼 위치를 원본에 맞추고, 문구가 비었거나 마지막으로 보낸 문구와 같으면 완료를 비활성화합니다. 기본값 복원은 완료 아래 텍스트 버튼으로 유지됩니다.
- 모듈 그림 자리는 원본 일러스트 파일을 확보할 때까지 비워 둡니다.

## 0.107.41+252 — 2026-09-16

### 추가
- 개체 신규 등록이 끝나면 "도마뱀 추가 완료" 화면을 보여주고 "기기 추가 하기"로 바로 기기 추가 흐름에 들어갈 수 있습니다. 뒤로가기로 폼에 돌아와 같은 개체를 다시 만들지 않습니다. 기존 개체 편집은 이전처럼 바로 닫힙니다.

### 변경
- 개체 정보 입력의 그룹 선택을 바텀시트에서 Figma 원본과 같은 전체 화면 카드 선택(그룹 번호·이름·사육장/카메라 아이콘·체크, 미선택 시 버튼 비활성, "나중에 하기"는 변경 없음)으로 바꿨습니다.

## 0.107.40+251 — 2026-09-16

### 변경
- 기기 추가(BLE 연결) 흐름의 하단 버튼을 Figma 원본처럼 콘텐츠 위에 고정했습니다. 기기 검색은 선택 전 "다시 스캔"(스캔 중 회색), 선택 후 "기기 N개 추가"로 바뀌고, 검색 중 표시는 제목 옆 작은 스피너로 옮겼습니다. 검색 결과가 없으면 회색 안내 박스를 표시합니다.
- 네트워크 선택 화면은 "네트워크 재검색"과 "직접 입력" 두 버튼, 오른쪽 위 닫기를 갖고, 비밀번호 화면에서 뒤로가기는 네트워크 목록으로 돌아갑니다. "다른 네트워크 선택" 행은 제거했습니다.
- Wi-Fi 연결 중에는 화면 전체를 흰색으로 덮고 스피너와 "WiFi 연결 중"을 표시합니다. 비밀번호 오류로 실패하면 입력을 유지한 채 확인 모달을 띄웁니다. 시간 초과·등록 대기 등 다른 결과는 기존 결과 화면을 유지합니다.
- 연결 완료 화면의 체크·제목·안내·버튼 위치와 개체 선택 화면의 제목 위치(한 마리는 더 아래), 성별 태그(24pt 알약), 사진 모서리, 하단 버튼을 원본 좌표에 맞췄습니다.
- 안내 문구를 원본과 같게 바꿨습니다: "다시 스캔", "검색된 기기가 없습니다", "기기에 연결할 네트워크를 선택하세요", "검색된 네트워크가 없습니다", "직접 입력".

## 0.107.39+250 — 2026-09-16

### 변경
- 영상 기기 저장 중에는 화면 전체를 흰색으로 덮고 원형 스피너와 "영상 다운로드 중"을 표시하며, 완료되면 "영상 다운로드 완료" 토스트를 띄웁니다. 실패는 기존 스낵바로 구분해 알립니다.
- 북마크·다운로드 토스트 폭이 글자 길이에 맞춰집니다.

## 0.107.38+249 — 2026-09-16

### 변경
- 영상 플레이어 가로 화면을 Figma 원본대로 영상 위에 겹치는 구성으로 바꿨습니다. 왼쪽 위 닫기 원형 버튼, 가운데 날짜·시각 알약, 오른쪽 위 삭제·다운로드·공유·메모·북마크 알약, 아래쪽 흰 그라데이션 위에 재생·시각·배속·확대와 왼쪽 정렬 필름스트립이 놓입니다. 화면을 탭하면 영상만 남습니다.
- 세로 화면의 하단 동작 순서를 다운로드 → 공유 → 메모 → 북마크로 바꾸고, 재생 컨트롤 행을 36pt(시각 14pt)로, 필름스트립을 홈 인디케이터 위 54pt 위치로 맞췄습니다.
- 필름스트립 썸네일을 40×40(모서리 4), 현재 항목 54×40, 현재 항목 양옆 간격 12, 양 끝 흐림으로 원본과 같게 했습니다.
- 영상을 두 번 탭해 10초 이동하면 탭한 쪽에 "+10s/-10s" 칩을 잠깐 표시합니다.
- 카메라 상세 공용 상단바의 하단선이 높이를 1pt 늘리던 문제를 고쳐 북마크·하이라이트·플레이어 본문이 원본 좌표(y130 등)에 맞습니다.

## 0.107.37+248 — 2026-09-16

### 추가
- 북마크 저장이 끝나면 "북마크에 저장되었습니다" 토스트(흰 알약, 체크 아이콘)를 표시합니다. 메모만 고친 경우에는 표시하지 않습니다.

### 변경
- 북마크 메모 카드의 더보기 메뉴를 Figma 원본과 같은 흰 시트(카드 오른쪽 정렬, 행 44, 삭제는 빨간 글자)로 바꿨습니다.
- 메모 창 제목을 새 메모·북마크 저장 흐름에서는 "메모 추가", 기존 메모 편집에서는 "메모 수정"으로 구분합니다.
- 북마크 목록 날짜 헤더의 줄높이와 메모 입력칸 안쪽 여백을 원본 좌표에 맞췄습니다. 가로 화면에서는 메모 창이 키보드 위 공간을 그대로 씁니다.

## 0.107.36+247 — 2026-09-16

### 수정
- 온습도 상세 주간 그래프를 Figma 원본에 맞췄습니다. 헤더 아이콘의 내부 그림이 보이고, 최고값 막대의 위·아래 수치가 강조색으로 표시되며, 요일은 강조 없이 회색으로 통일했습니다.
- 주간 그래프 격자를 가로 8줄·세로 열 경계선으로 그리고, Y 눈금 7개를 눈금선에 정렬했습니다. 막대 수치 글자를 14pt로 키우고 최저 막대 색을 원본(#B4AEAE)으로 맞췄습니다.
- 온습도 상세의 세그먼트·날짜·헤더·차트·제어 기록이 원본 좌표와 1pt 어긋나던 문제(상단선이 높이를 더하던 구조)를 고쳤습니다.
- 사육장 제어 기록의 제목·항목 줄높이를 원본과 같게 해 행 간격을 60pt로 맞추고 패널 상단선을 추가했습니다. 일간 차트 마커를 띠 가운데에 배치했습니다.

## 0.107.35+246 — 2026-09-16

### 변경
- 홈·카메라·MyCre 빈 화면의 추가 버튼을 Figma 원본과 같은 둥근 알약형(+ 아이콘, 글자 폭 기준)으로 통일했습니다. MyCre 미등록 상태의 검정 전폭 버튼도 같은 빨간 버튼으로 바뀝니다.
- 빈 화면 안내 문구 색을 원본(#626262)에 맞췄습니다.
- 기기 관리에 그룹이 없을 때 안내를 회색 박스로 표시하고 문구를 "지정된 그룹이 없습니다"로 줄였습니다.

## 0.107.34+245 — 2026-09-16

### 추가
- 모프 검색에서 `ㄹ`처럼 초성만 입력해도 이름의 모든 음절 초성을 검사해 찾습니다. 한글·영문 검색은 그대로 동작합니다.
- 검색 결과에서 일치한 글자를 강조색으로 표시하고, 검색창 오른쪽에 검색어 지우기 버튼을 두었습니다. 지우기는 검색어만 비우고 이미 선택한 모프는 유지합니다.
- 검색 결과가 없으면 "일치하는 모프가 없습니다" 안내를 표시합니다.

### 변경
- 검색어가 있을 때는 초성 구분 제목 없이 결과 목록만 보여주며, 일치 위치가 앞선 순서로 정렬합니다.

## 0.107.33+244 — 2026-09-16

### 수정
- 모프 선택을 전체 화면과 초성별 한글 목록으로 맞추고 초기화 항목을 제거했습니다.
- 모프 선택 시 바로 반영하며 뒤로가기는 기존 값을 유지합니다.

## 0.107.32+243 — 2026-09-16

### 수정
- 종 선택 메뉴 글자 자간을 Figma 원본에 맞췄습니다.

## 0.107.31+242 — 2026-09-16

### 수정
- 개체 입력 저장 버튼을 콘텐츠 위에 고정하고 키보드와 마지막 메모칸을 가리지 않도록 여백을 조정했습니다.
- 이름 오류 문구의 위치·자간과 다음 입력칸 간격을 Figma에 맞췄습니다.

## 0.107.30+241 — 2026-09-16

### 수정
- 개체 사진 교체·삭제 메뉴를 사진 아래 팝업으로 변경하고 연필 크기와 입력값 정렬을 맞췄습니다.
- 날짜 초기화를 선택창으로 옮기고 체중 단위를 표시하되 숫자와 소수 정밀도는 그대로 저장합니다.

## 0.107.29+240 — 2026-09-16

### 수정
- 개체 입력은 이름·종과 입력값이 유효할 때만 저장 버튼을 활성화합니다.
- 메모칸 높이, 뒤로가기 위치, 그룹 화살표와 카운터 여백, 선택·저장 글자 스타일을 Figma에 맞췄습니다.

## 0.107.28+239 — 2026-09-16

### 수정
- 개체 관리 목록의 우측 닫기, 사진 크기, 이름·성별 배지와 체중 표기를 Figma에 맞췄습니다.
- 더보기 메뉴의 위치·크기·서체·색상과 하단 개체 추가 버튼을 원본 배치로 정리했습니다.

## 0.107.27+238 — 2026-09-16

### 수정
- 개체 삭제 확인창을 Figma 공통 팝업으로 통일하고 실제 개체 이름과 조사를 표시합니다.
- 본문 중앙 정렬과 동일 너비의 취소·삭제 버튼을 적용했습니다.

## 0.107.26+237 — 2026-09-16

### 수정
- 그룹 삭제 확인 문구를 Figma의 한 줄 문장으로 맞춰 팝업 높이와 가운데 정렬을 정리했습니다.

## 0.107.25+236 — 2026-09-16

### 수정
- 관리 확인창 버튼의 글자 잘림과 크기, 팝업 모서리를 Figma에 맞췄습니다.
- 그룹 이동 안내의 한글·숫자 이름 조사를 구분하고 새 그룹 문구 중복을 제거했습니다.

## 0.107.24+235 — 2026-09-16

### 수정
- 기기 관리의 그룹 카드 간격과 다음 섹션 여백을 Figma 기준으로 맞췄습니다.
- 그룹 없음 목록에 공통 회색 배경과 둥근 외곽을 적용했습니다.

## 0.107.23+234 — 2026-09-16

### 수정
- 그룹 최종 확인·상세 화면의 추가·변경 버튼 내부 여백과 글자 줄높이를 Figma 기준으로 맞췄습니다.

## 0.107.22+233 — 2026-09-16

### 수정
- 그룹 이름 오류 안내를 Figma 문구·서체·색상·간격으로 맞추고 입력창의 회색 테두리를 유지합니다.
- 이름 검증 오류가 있으면 진행 버튼을 비활성화하고, 수정 중 재검증해 올바른 이름을 입력하면 다시 진행할 수 있게 했습니다.

## 0.107.21+232 — 2026-09-16

### 수정
- 기기 그룹 설정의 그룹 추가 글자 굵기·줄높이와 그룹에서 제외 터치 너비를 Figma에 맞췄습니다.
- 공통 관리 화면 제목과 주요 버튼 글자색을 기존 ColorSystem 기준으로 보정했습니다.

## 0.107.20+231 — 2026-09-16

### 수정
- 사육장·카메라 상세의 전원/그룹 아이콘 크기, 전원 상태 간격, 그룹 설정 정렬과 삭제 버튼 줄높이·터치 너비를 Figma에 맞췄습니다.
- 공통 관리 화면의 뒤로 버튼 위치, 입력 글자 수 우측 여백, 주요 버튼 글자 줄높이를 보정했습니다.

## 0.107.19+230 — 2026-09-16

### 수정
- 그룹 구성원 선택 버튼을 목록 위에 고정해 스크롤 내용이 버튼 뒤로 지나가도록 변경했습니다. 마지막 항목도 버튼 위로 올려 선택할 수 있으며, 미선택 시 비활성 조건은 유지합니다.

## 0.107.18+229 — 2026-09-16

### 수정
- 그룹 구성원 추가·변경 화면에 사육장·카메라·개체별 연회색 목록 배경과 둥근 모서리를 적용해 Figma 디자인에 맞췄습니다.

## 0.107.17+228 — 2026-09-16

### 변경
- MyCre 활동 그래프를 현재 그룹에 연결된 카메라 기준으로 표시합니다. 개체 등록일이나 과거 연결 이력으로 조회 기간을 자르지 않습니다.
- 카메라 교체 시 선택한 날짜는 유지하면서 새 카메라의 기록으로 전환합니다. 이전 카메라 기록은 이어 붙이지 않으며, 카메라가 미연결이면 기존 초기 화면을 표시합니다.
- MyCre 그래프의 연결 이력 테이블 조회 의존성을 제거했습니다. 평균 계산 기준과 서버 데이터는 변경하지 않았습니다.

## 0.107.16+227 — 2026-09-16

### 제거
- MyCre 그래프 하단과 개체 미등록 화면에서 이전 야간 리포트 보기 버튼을 제거했습니다. 저장된 기록과 그래프 집계 방식은 변경하지 않았습니다.

## 0.107.15+226 — 2026-09-16

### 수정
- MyCre 일간·주간 그래프의 최저값 막대를 Figma의 연한 회색으로 구분합니다. 최고값 막대·텍스트는 빨강, 일반 막대와 최저값 텍스트는 진회색을 유지합니다.

## 0.107.14+225 — 2026-09-16

### 제거
- Figma에 맞춰 MyCre 주간 그래프 아래의 카메라 감지·개체 구별 안내문을 제거했습니다.

## 0.107.13+224 — 2026-09-16

### 수정
- MyCre 주간 그래프의 최고값 시간 텍스트를 해당 막대와 같은 빨간색으로 표시합니다.

## 0.107.12+223 — 2026-09-15

### 수정
- MyCre 일간·주간 그래프 아래의 관측 완료 여부 안내문과 해당 여백을 제거해 주간 요약이 아래로 밀리는 현상을 줄였습니다. 평균을 계산할 수 없는 경우의 `--` 표시는 유지합니다.

## 0.107.11+222 — 2026-09-15

### 수정
- 종 선택 목록을 입력칸 아래에 열리는 흰색·12px 둥근 팝업으로 변경했습니다. 신규 등록은 크레스티드 게코만 선택하며 기존 다른 종은 유지합니다.
- 큰 글자에서도 긴 기존 종 이름을 표시할 수 있도록 입력칸 높이를 조정했습니다. 선택 및 팝업 취소 동작을 검증했습니다.

## 0.107.10+221 — 2026-09-15

### 수정
- 기기 연결 후 등록된 도마뱀을 카드로 선택하는 화면을 Figma대로 연결했습니다. 선택300·미선택400 SVG, 사진·성별·모프·체중을 표시합니다.
- 개체 교체 시 기존 사육장과 카메라를 유지하고, 다른 그룹에서 이동할 때 확인합니다. 실패하면 선택을 유지하며 나중에 하기는 저장하지 않습니다.
- 기기 검색 목록의 배경·구분선·신호 세기 SVG와 안내 문구를 맞췄습니다. Wi-Fi 입력 테두리·눈 아이콘·기억하기 간격과 우측 닫기 버튼을 수정했습니다.
- 기본 버튼 높이는 유지하되 큰 글자에서 긴 문구가 잘리지 않도록 높이가 늘어나게 했습니다.

## 0.107.9+220 — 2026-09-15

### 수정
- 디자이너 원본에 맞춰 Wi-Fi 비밀번호 기억하기는 300 계열, 기기 그룹 설정은 선택300·미선택400 계열로 구분했습니다.
- 기기 검색·Wi-Fi·그룹 구성원 선택의 체크박스를 선택 시 빨강, 미선택 시 비활성 회색으로 통일했습니다. 기존 컬러 시스템과 SVG를 사용합니다.

## 0.107.8+219 — 2026-09-15

### 수정
- 온습도 그래프의 좌우 여백과 눈금 간격·색상을 Figma에 맞추고, 측정선·선택점 좌표를 함께 조정했습니다.
- MyCre 일간·주간 격자를 얇게 통일하고 주간 세로 격자와 우측 눈금 영역의 가로선을 복원했습니다. 실제 활동량 집계 단위는 유지합니다.
- 그룹 삭제·제외 문구를 18px SemiBold로 맞추고 기기 ID의 우측 정렬·글자색을 수정했습니다.
- 개체 등록 종 선택 화살표에 기존 비활성 아이콘 색상을 적용했습니다.
- 카메라 미연결이 확인되면 연결된 카메라의 활동이라는 안내를 숨깁니다.

## 0.107.7+218 — 2026-09-15

### 수정
- 카메라 미연결이 확인된 MyCre만 일간·주간 총 활동과 평균을 0h 0m 및 빈 그래프로 표시합니다. 로딩·조회 오류·관측 결측은 미연결0으로 바꾸지 않습니다.
- 카메라 연결 안내와 현재 개체를 선택한 그룹 구성원 선택 진입점을 추가했습니다. CTA 전경색은 VIVA 컬러 시스템 역할로 관리하며 초기0을 측정 데이터로 저장하지 않습니다.

## 0.107.6+217 — 2026-09-15

### 수정
- 개체 폼의 사진 추가 아이콘에 지정된 비활성 색을 적용하고 성별 비선택 영역 사이의 구분선을 맞췄습니다.

## 0.107.5+216 — 2026-09-15

### 수정
- 홈·카메라·MyCre 공통 드롭다운에 원본 V자 SVG와 200px 메뉴, 선택 Bold·선택됨·구분선을 반영했습니다. 긴 텍스트의 가로 넘침도 방지했습니다.

## 0.107.4+215 — 2026-09-15

### 수정
- 일간 온습도 최상단 눈금 잘림을 고치고 시간 세로 격자·외곽선을 복원했습니다.
- 분무 원본 SVG 글리프를 보존하면서 마커·제어 기록 배경을 VIVA 파랑 역할색으로 적용했습니다.

## 0.107.3+214 — 2026-09-15

### 수정
- 그룹 설정의 종류별 아이콘·추가 카드·여백과 ON 고정 전원 표시를 Figma에 맞췄습니다.
- 그룹만 삭제하고 구성원을 보존하는 확인창·단일 RPC 요청·재시도·실패 복구를 구현했습니다. 운영 RPC 배포는 이관훈님 작업으로 남아 있으며, SQL은 검토용 초안과 격리 검증만 완료했습니다.

## 0.107.2+213 — 2026-09-15

### 수정
- 시뮬레이터 비교에서 승인한 16개 항목 반영: 그룹 목록 카드·상세 이동, 기기 관리 빈 상태 카드, 상세 행 간격과 화살표.
- 그룹 제외 기능을 그룹 설정 화면으로 이동하고, 기기 상세의 추가 온라인 문구 제거. 기존 ON 고정 및 소속 해제 확인 동작 유지.
- 기존 VIVA 컬러 시스템으로 일간 온습도 선 색상과 굵기 조정, MyCre 막대 상단 라운드와 시간축 점선 복원.
- 개체 등록 입력칸 테두리·원본 체크 SVG 비율·검색/달력 아이콘 색상 수정.
- 북마크 날짜에 업데이트 접두사를 항상 표시하고, 공개 시각 미지원 하이라이트는 임시로 ‘업데이트 없음’ 표시.

이 파일은 비바나트 앱의 릴리스별 변경 사항을 한글로 기록하는 단일 패치노트입니다.

- 최신 버전을 위에 기록합니다.
- 제목은 `## [0.107.1+212] - 2026-09-15

### 수정

- 기기 추가·관리, 그룹 추가·편집, 개체 관리·기존 리포트 경로를 새 화면에 연결했습니다.
- 정적 관리 경로가 개체 ID 경로에 가려지지 않도록 순서를 정리하고 기존 딥링크를 유지했습니다.
- 연결 완료에서 그룹 ID를 새 개체 등록에 전달하고, 기존 기기 연결에서는 선택한 기기를 그룹 편집에 미리 반영합니다.

## [0.107.0+211] - 2026-09-15

### 추가

- 홈·카메라·MyCre 화면을 승인 Figma에 맞추고 그룹 이름 우선 선택, 기기·그룹·개체 관리 화면과 통합 BLE 추가 흐름을 구현했습니다.
- 모든 영상의 로컬 메모 편집과 북마크 전용 메모 카드, 새 메모 연속 동일색 방지를 추가했습니다.
- MyCre 일간·주간 활동 집계와 기존 리포트 진입, 크레스티드 등록·기존 개체 편집·사진·선택 그룹 폼을 추가했습니다.

### 변경

- 사육장만 있으면 홈의 라이브 공간을 생략하고 빈 상태는 원본 3배율 이미지를 사용합니다.
- 서버 원장에 숨김을 저장한 뒤 영상 목록과 재생 큐에서 제외합니다. 원본 영상·북마크·메모는 지우지 않으며, 운영 원장 미적용 시 실패를 안내합니다.
- 등록 부분 성공은 유지하고 불확실한 등록을 중복 전송하지 않습니다. 관리 전원 표시는 승인 사양대로 ON 고정입니다.

### 수정

- 원본 44px 관리 SVG와 온습도 다색 배지의 표시를 바로잡고, 작은 화면 플레이어·개체 입력 칸의 넘침과 간격을 보정했습니다.
- 제어 기록 조회 실패를 빈 기록과 구분하고 재시도할 수 있게 했습니다.

## [0.106.0+210] - 2026-09-15

### 추가

- 재설계용 활동 구간·배정 이력, 그룹·개체 원자 저장, 계정별 영상 숨김 저장소와 검토용 SQL 계약을 추가했습니다. 운영 DB에는 아직 적용하지 않았습니다.
- 크레스티드 등록 카탈로그와 기존 종·모프 보존 모델을 추가했습니다.

### 수정

- Wi-Fi 저장을 계정·SSID별로 격리하고 BLE 등록 확인에 실제 소유 UUID와 PAIR_OK를 사용합니다. 자격증명이 BLE 진단 로그에 남지 않게 했습니다.
- 제어 기록의 기간 전체 조회와 액추에이터별 직전 상태 조회를 추가하고 실패를 빈 기록으로 숨기지 않습니다.
- 개체 삭제 SQL은 원본 사진·이벤트·영상을 보존하는 방식이며, 계정 간 접근 차단과 재시도를 격리 DB에서 검증했습니다.

## [버전+빌드] - YYYY-MM-DD` 형식을 사용합니다.
- `추가`, `변경`, `수정`, `제거`, `보안` 중 필요한 분류만 작성합니다.
- 앱 동작에 반영되지 않은 계획은 완료된 변경으로 기록하지 않습니다.

## [0.105.0+209] - 2026-09-15

### 추가

- 홈 냉각팬을 실제 fan2 상태·켜기/끄기·시간 선택·일정에 연결했습니다. 미보고 상태는 `--`로 표시하고 제어를 비활성화합니다.
- 환기팬과 냉각팬의 기억한 작동 시간, 진행 타이머, 로컬 알림을 분리했습니다.

### 변경

- LED 밝기 선택을 20~100%, 10% 단위로 제한했습니다. 타일에는 실제 보고값을 유지하며 적용 전에는 명령을 보내지 않습니다.
- LCD 문구는 공백 포함 최대 20자로 제한하고, 전송 중 중복 적용을 막았습니다. 실패 시 입력을 유지합니다.
- 제어 기록과 차트 마커에 새 Figma 36px·28px SVG를 연결했습니다.

### 수정

- ACK 수신만으로 실행 성공으로 판단하지 않도록 보완했습니다. 결과가 `ok`인 명령만 성공 기록에 포함하고, 실패한 종료 명령이 기존 팬 타이머를 취소하지 않게 했습니다.

## [0.104.0+208] - 2026-09-15

### 추가

- 새 Figma SVG 70개의 Flutter용 사본과 3배율 PNG 4개의 에셋 등록을 추가했습니다. 원본은 그대로 보존하고 Flutter용 SVG의 알파 마스크만 정규화했습니다.
- 카메라 탭의 하이라이트·북마크 진입 아이콘을 새 원본 사본에 연결했습니다. 나머지 화면별 에셋 교체는 후속 구현에서 진행합니다.

### 수정

- 라이트 테마의 화면·헤더 배경을 확정 디자인의 흰색으로 맞췄습니다.
- 내용을 스크롤해도 기본 AppBar에 그림자와 표면 색이 추가되지 않도록 했습니다. Android 실기기 확인은 후속 검수에 포함합니다.

## [0.103.2+207] - 2026-09-15

### 변경

- Android FCM 작업의 운영 상태, 보안 처리, 외부 인계와 남은 단계를 한 번에 재개할 수 있는 체크포인트 문서를 추가했습니다.
- 이관훈님과 petcam-lab 담당자에게 각각 전달할 FCM 연동 Slack 스크립트를 분리해 추가했습니다.
- Android FCM 저장소 마이그레이션과 알림 수집·발송 Edge Function을 운영 Supabase에 배포했습니다.
- 예약된 푸시 발송을 1분 주기로 처리하도록 Supabase Vault, `pg_net`, `pg_cron`을 연결했습니다. Android 실기기 수신 검증은 후속 단계로 남아 있습니다.

### 보안

- 발송 함수는 전용 Supabase secret API key를 `apikey` 헤더로만 검증하며, 키는 Vault에 보관하도록 변경했습니다.
- Firebase Admin 서비스 계정은 `FIREBASE_SERVICE_ACCOUNT_JSON` 이름의 Supabase Edge Function Secret에 등록하고 저장소·문서에는 값을 남기지 않았습니다.
- 외부 알림 생산자용 `PUSH_EVENT_INGEST_SECRET`을 Supabase Secret에 등록하고 안전한 전달을 위해 Vault에도 보관했습니다.
- 로그아웃 시 서버 알림 연결 해제에 이어 기기의 FCM 토큰도 삭제하도록 보강했습니다. 서버 연결 해제 실패 시에도 토큰 삭제를 시도합니다.
- 세션 만료 등 강제 로그아웃도 토큰을 삭제하며, 삭제 중 토큰 갱신이나 새 로그인으로 이전 토큰이 다시 등록되지 않도록 처리 순서를 보호했습니다.
- 같은 프레임의 로그아웃·로그인이 하나의 계정 전환으로 합쳐져도 이전 FCM 토큰을 삭제한 후 새 계정 토큰을 등록하도록 보강했습니다.

## [0.103.1+206] - 2026-09-15

### 수정

- 로그아웃 중 앱 복귀나 토큰 갱신이 기기 알림 연결을 다시 활성화하지 않도록 처리 순서를 보호했습니다.
- 푸시 확인은 현재 계정의 해당 알림 한 건만 조회하며, 알림 원본이 잠시 조회되지 않은 경우 다음 수신·탭에서 다시 시도합니다.
- 알림 목록과 읽음 상태가 즉시 갱신되도록 Supabase Realtime publication 등록을 보완했습니다.
- 기존 iOS 14 앱의 의존성 호환성을 유지하도록 Firebase 패키지 버전을 조정했습니다. iOS 푸시·APNs는 활성화하지 않았습니다.

## [0.103.0+205] - 2026-09-15

### 추가

- Android FCM 알림 권한 안내와 재시도, 설치별 토큰 등록·갱신, 로그아웃 전 기기 연결 해제를 추가했습니다.
- 포그라운드 알림 표시, 백그라운드·종료 상태의 알림 탭 이동과 긴급·안전 전용 알림 채널을 추가했습니다.
- Supabase 실시간 알림 센터에 최신순 목록, 개별·전체 읽음, 오류 재시도와 프로필 미읽음 표시를 연결했습니다.
- 서버 이벤트 수집·중복 방지·예약 발송을 위한 알림 이벤트와 outbox 계약을 추가했습니다. 운영 푸시 활성화는 서버 배포·비밀 값 설정·Android 실기기 수신 검증 후 진행합니다.

### 보안

- 알림 조회·읽음·토큰 등록을 현재 계정으로 격리하고, 알림 탭은 허용된 내부 경로만 이동하도록 검증합니다.

## [0.102.1+204] - 2026-09-15

### 변경

- Android 앱을 Firebase 프로젝트 `vivanaut-app`에 연결했습니다.

## [0.102.0+203] - 2026-09-15

### 변경

- 영문 브랜드, Dart 패키지명, 플랫폼 식별자를 `vivanaut`으로 정정했습니다.

## [0.101.5+202] - 2026-09-15

### 변경

- 카메라 활동 영상을 시간대별로 끊지 않고, 현재 조회 중인 전체 피드 순서대로 플레이어에서 이어 볼 수 있게 했습니다.
- 플레이어가 재생목록 끝에 가까워지면 다음 영상 페이지를 미리 불러오도록 개선했습니다.
